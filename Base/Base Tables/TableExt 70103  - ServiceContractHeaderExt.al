tableextension 70103 ServiceContractHeaderExt extends "Service Contract Header"
{
    // version NAVW19.00.00.50221,NAVIN9.00.00.50221,TFS225680

    fields
    {

        //Unsupported feature: Change OptionString on "Status(Field 5)". Please convert manually.


        //Unsupported feature: Change Editable on "Status(Field 5)". Please convert manually.


        //Unsupported feature: Change Editable on ""Change Status"(Field 6)". Please convert manually.


        //Unsupported feature: Change NotBlank on ""Customer No."(Field 7)". Please convert manually.


        //Unsupported feature: Change CalcFormula on "Name(Field 8)". Please convert manually.


        //Unsupported feature: Change Editable on "Name(Field 8)". Please convert manually.


        //Unsupported feature: Change CalcFormula on "Address(Field 9)". Please convert manually.


        //Unsupported feature: Change Editable on "Address(Field 9)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Address 2"(Field 10)". Please convert manually.


        //Unsupported feature: Change Editable on ""Address 2"(Field 10)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Post Code"(Field 11)". Please convert manually.


        //Unsupported feature: Change Editable on ""Post Code"(Field 11)". Please convert manually.


        //Unsupported feature: Change CalcFormula on "City(Field 12)". Please convert manually.


        //Unsupported feature: Change Editable on "City(Field 12)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Salesperson Code"(Field 15)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Bill-to Name"(Field 17)". Please convert manually.


        //Unsupported feature: Change Editable on ""Bill-to Name"(Field 17)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Bill-to Address"(Field 18)". Please convert manually.


        //Unsupported feature: Change Editable on ""Bill-to Address"(Field 18)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Bill-to Address 2"(Field 19)". Please convert manually.


        //Unsupported feature: Change Editable on ""Bill-to Address 2"(Field 19)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Bill-to Post Code"(Field 20)". Please convert manually.


        //Unsupported feature: Change Editable on ""Bill-to Post Code"(Field 20)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Bill-to City"(Field 21)". Please convert manually.


        //Unsupported feature: Change Editable on ""Bill-to City"(Field 21)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Ship-to Code"(Field 22)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Ship-to Name"(Field 23)". Please convert manually.


        //Unsupported feature: Change Editable on ""Ship-to Name"(Field 23)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Ship-to Address"(Field 24)". Please convert manually.


        //Unsupported feature: Change Editable on ""Ship-to Address"(Field 24)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Ship-to Address 2"(Field 25)". Please convert manually.


        //Unsupported feature: Change Editable on ""Ship-to Address 2"(Field 25)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Ship-to Post Code"(Field 26)". Please convert manually.


        //Unsupported feature: Change Editable on ""Ship-to Post Code"(Field 26)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Ship-to City"(Field 27)". Please convert manually.


        //Unsupported feature: Change Editable on ""Ship-to City"(Field 27)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Invoice Period"(Field 32)". Please convert manually.


        //Unsupported feature: Change Editable on ""Last Invoice Date"(Field 33)". Please convert manually.


        //Unsupported feature: Change Editable on ""Next Invoice Date"(Field 34)". Please convert manually.


        //Unsupported feature: Change BlankZero on ""Max. Labor Unit Price"(Field 39)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Calcd. Annual Amount"(Field 40)". Please convert manually.


        //Unsupported feature: Change Editable on ""Calcd. Annual Amount"(Field 40)". Please convert manually.


        //Unsupported feature: Change BlankZero on ""Annual Amount"(Field 42)". Please convert manually.


        //Unsupported feature: Change BlankZero on ""Amount per Period"(Field 43)". Please convert manually.


        //Unsupported feature: Change Editable on ""Amount per Period"(Field 43)". Please convert manually.


        //Unsupported feature: PropertyModification on ""Next Invoice Period"(Field 46)". Please convert manually.


        //Unsupported feature: Change Editable on ""Last Price Update Date"(Field 51)". Please convert manually.


        //Unsupported feature: Change Editable on ""Last Price Update %"(Field 53)". Please convert manually.


        //Unsupported feature: Change BlankZero on ""Response Time (Hours)"(Field 55)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""No. of Posted Invoices"(Field 57)". Please convert manually.


        //Unsupported feature: Change Editable on ""No. of Posted Invoices"(Field 57)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""No. of Unposted Invoices"(Field 58)". Please convert manually.


        //Unsupported feature: Change Editable on ""No. of Unposted Invoices"(Field 58)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Quote Type"(Field 63)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Shortcut Dimension 1 Code"(Field 67)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Shortcut Dimension 2 Code"(Field 68)". Please convert manually.


        //Unsupported feature: Change Editable on ""No. Series"(Field 82)". Please convert manually.


        //Unsupported feature: Change CalcFormula on "Comment(Field 84)". Please convert manually.


        //Unsupported feature: Change Editable on "Comment(Field 84)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Bill-to County"(Field 89)". Please convert manually.


        //Unsupported feature: Change Editable on ""Bill-to County"(Field 89)". Please convert manually.


        //Unsupported feature: Change CalcFormula on "County(Field 90)". Please convert manually.


        //Unsupported feature: CodeModification on "County(Field 90)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Ship-to County"(Field 91)". Please convert manually.


        //Unsupported feature: Change Editable on ""Ship-to County"(Field 91)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Country/Region Code"(Field 92)". Please convert manually.


        //Unsupported feature: Change Editable on ""Country/Region Code"(Field 92)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Bill-to Country/Region Code"(Field 93)". Please convert manually.


        //Unsupported feature: Change Editable on ""Bill-to Country/Region Code"(Field 93)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Ship-to Country/Region Code"(Field 94)". Please convert manually.


        //Unsupported feature: Change Editable on ""Ship-to Country/Region Code"(Field 94)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Name 2"(Field 95)". Please convert manually.


        //Unsupported feature: Change Editable on ""Name 2"(Field 95)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Bill-to Name 2"(Field 96)". Please convert manually.


        //Unsupported feature: Change Editable on ""Bill-to Name 2"(Field 96)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Ship-to Name 2"(Field 97)". Please convert manually.


        //Unsupported feature: Change Editable on ""Ship-to Name 2"(Field 97)". Please convert manually.


        //Unsupported feature: Change Editable on ""Next Invoice Period Start"(Field 98)". Please convert manually.


        //Unsupported feature: Change Editable on ""Next Invoice Period End"(Field 99)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Contract Invoice Amount"(Field 100)". Please convert manually.


        //Unsupported feature: Change Editable on ""Contract Invoice Amount"(Field 100)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Contract Prepaid Amount"(Field 101)". Please convert manually.


        //Unsupported feature: Change Editable on ""Contract Prepaid Amount"(Field 101)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Contract Discount Amount"(Field 102)". Please convert manually.


        //Unsupported feature: Change Editable on ""Contract Discount Amount"(Field 102)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Contract Cost Amount"(Field 103)". Please convert manually.


        //Unsupported feature: Change Editable on ""Contract Cost Amount"(Field 103)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Contract Gain/Loss Amount"(Field 104)". Please convert manually.


        //Unsupported feature: Change Editable on ""Contract Gain/Loss Amount"(Field 104)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""No. of Posted Credit Memos"(Field 106)". Please convert manually.


        //Unsupported feature: Change Editable on ""No. of Posted Credit Memos"(Field 106)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""No. of Unposted Credit Memos"(Field 107)". Please convert manually.


        //Unsupported feature: Change Editable on ""No. of Unposted Credit Memos"(Field 107)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Type Filter"(Field 140)". Please convert manually.


        //Unsupported feature: Change Editable on ""Dimension Set ID"(Field 480)". Please convert manually.


        //Unsupported feature: CodeModification on ""Contract No."(Field 1).OnValidate". Please convert manually.

        //trigger "(Field 1)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Contract No." <> xRec."Contract No." THEN BEGIN
          ServMgtSetup.GET;
          NoSeriesMgt.TestManual(ServMgtSetup."Service Contract Nos.");
          "No. Series" := '';
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Contract No." <> xRec."Contract No." then begin
          ServMgtSetup.Get;
          NoSeriesMgt.TestManual(ServMgtSetup."Service Contract Nos.");
          "No. Series" := '';
        end;
        */
        //end;


        //Unsupported feature: CodeModification on "Status(Field 5).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF Status <> xRec.Status THEN BEGIN
          IF NOT SuspendChangeStatus THEN
            TESTFIELD("Change Status","Change Status"::Open);
          CASE "Contract Type" OF
            "Contract Type"::Contract:
              BEGIN
                IF Status <> Status::Canceled THEN
                  ERROR(Text006,FIELDCAPTION(Status));

                CALCFIELDS("No. of Unposted Invoices","No. of Unposted Credit Memos");
                CASE TRUE OF
                  ("No. of Unposted Invoices" <> 0) AND ("No. of Unposted Credit Memos" = 0):
                    IF NOT ConfirmManagement.ConfirmProcess(Text048,TRUE) THEN BEGIN
                      Status := xRec.Status;
                      EXIT;
                    END;
                  ("No. of Unposted Invoices" = 0) AND ("No. of Unposted Credit Memos" <> 0):
                    IF NOT ConfirmManagement.ConfirmProcess(Text049,TRUE) THEN BEGIN
                      Status := xRec.Status;
                      EXIT;
                    END;
                  ("No. of Unposted Invoices" <> 0) AND ("No. of Unposted Credit Memos" <> 0):
                    IF NOT ConfirmManagement.ConfirmProcess(Text055,TRUE) THEN BEGIN
                      Status := xRec.Status;
                      EXIT;
                    END;
                END;

                ServMgtSetup.GET;
                IF ServMgtSetup."Use Contract Cancel Reason" THEN
                  TESTFIELD("Cancel Reason Code");

                ServLedgEntry.SETRANGE(Type,ServLedgEntry.Type::"Service Contract");
                ServLedgEntry.SETRANGE("No.","Contract No.");
                ServLedgEntry.SETRANGE("Moved from Prepaid Acc.",FALSE);
                ServLedgEntry.SETRANGE(Open,FALSE);
                ServLedgEntry.CALCSUMS("Amount (LCY)");
                IF ServLedgEntry."Amount (LCY)" <> 0 THEN
                  StrToInsert := OpenPrepaymentEntriesExistTxt;
                IF NOT ConfirmManagement.ConfirmProcess(
                     STRSUBSTNO(CancelTheContractQst,StrToInsert),TRUE)
                THEN BEGIN
                  Status := xRec.Status;
                  EXIT;
                END;
                FiledServContract.FileContractBeforeCancellation(xRec);
              END;
            "Contract Type"::Quote:
              CASE Status OF
                Status::" ":
                  IF xRec.Status = xRec.Status::Canceled THEN BEGIN
                    ServContractLine.RESET;
                    ServContractLine.SETRANGE("Contract Type","Contract Type");
                    ServContractLine.SETRANGE("Contract No.","Contract No.");
                    IF ServContractLine.FIND('-') THEN
                      REPEAT
                        ServContractLine2.RESET;
                        ServContractLine2.SETCURRENTKEY("Service Item No.");
                        ServContractLine2.SETRANGE("Service Item No.",ServContractLine."Service Item No.");
                        ServContractLine2.SETRANGE("Contract Type","Contract Type"::Contract);
                        IF ServContractLine2.FINDFIRST THEN BEGIN
                          AnyServItemInOtherContract := TRUE;
                          ServContractLine.MARK(TRUE);
                        END;
                      UNTIL ServContractLine.NEXT = 0;

                    "Change Status" := "Change Status"::Open;

                    IF AnyServItemInOtherContract THEN
                      IF ConfirmManagement.ConfirmProcessUI(
                           STRSUBSTNO(Text062,FORMAT(xRec.Status),FIELDCAPTION(Status)),TRUE)
                      THEN BEGIN
                        ServContractLine.MARKEDONLY(TRUE);
                        PAGE.RUNMODAL(PAGE::"Service Contract Line List",ServContractLine);
                      END;
                  END;
                Status::Signed:
                  ERROR(
                    Text009,
                    FIELDCAPTION(Status),Status,FIELDCAPTION("Contract Type"),"Contract Type");
                Status::Canceled:
                  IF NOT ConfirmManagement.ConfirmProcess(STRSUBSTNO(Text010,"Contract No."),TRUE) THEN BEGIN
                    Status := xRec.Status;
                    EXIT;
                  END;
              END;
          END;
          IF Status = Status::Canceled THEN
            "Change Status" := "Change Status"::Locked;
          ServContractLine.RESET;
          ServContractLine.SETRANGE("Contract Type","Contract Type");
          ServContractLine.SETRANGE("Contract No.","Contract No.");
          ServContractLine.MODIFYALL("Contract Status",Status);
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if Status <> xRec.Status then begin
          if not SuspendChangeStatus then
            TestField("Change Status","Change Status"::Open);
          case "Contract Type" of
            "Contract Type"::Contract:
              begin
                if Status <> Status::Canceled then
                  Error(Text006,FieldCaption(Status));

                CalcFields("No. of Unposted Invoices","No. of Unposted Credit Memos");
                case true of
                  ("No. of Unposted Invoices" <> 0) and ("No. of Unposted Credit Memos" = 0):
                    if not ConfirmManagement.ConfirmProcess(Text048,true) then begin
                      Status := xRec.Status;
                      exit;
                    end;
                  ("No. of Unposted Invoices" = 0) and ("No. of Unposted Credit Memos" <> 0):
                    if not ConfirmManagement.ConfirmProcess(Text049,true) then begin
                      Status := xRec.Status;
                      exit;
                    end;
                  ("No. of Unposted Invoices" <> 0) and ("No. of Unposted Credit Memos" <> 0):
                    if not ConfirmManagement.ConfirmProcess(Text055,true) then begin
                      Status := xRec.Status;
                      exit;
                    end;
                end;

                ServMgtSetup.Get;
                if ServMgtSetup."Use Contract Cancel Reason" then
                  TestField("Cancel Reason Code");

                ServLedgEntry.SetRange(Type,ServLedgEntry.Type::"Service Contract");
                ServLedgEntry.SetRange("No.","Contract No.");
                ServLedgEntry.SetRange("Moved from Prepaid Acc.",false);
                ServLedgEntry.SetRange(Open,false);
                ServLedgEntry.CalcSums("Amount (LCY)");
                if ServLedgEntry."Amount (LCY)" <> 0 then
                  StrToInsert := OpenPrepaymentEntriesExistTxt;
                if not ConfirmManagement.ConfirmProcess(
                     StrSubstNo(CancelTheContractQst,StrToInsert),true)
                then begin
                  Status := xRec.Status;
                  exit;
                end;
                FiledServContract.FileContractBeforeCancellation(xRec);
              end;
            "Contract Type"::Quote:
              case Status of
                Status::" ":
                  if xRec.Status = xRec.Status::Canceled then begin
                    ServContractLine.Reset;
                    ServContractLine.SetRange("Contract Type","Contract Type");
                    ServContractLine.SetRange("Contract No.","Contract No.");
                    if ServContractLine.Find('-') then
                      repeat
                        ServContractLine2.Reset;
                        ServContractLine2.SetCurrentKey("Service Item No.");
                        ServContractLine2.SetRange("Service Item No.",ServContractLine."Service Item No.");
                        ServContractLine2.SetRange("Contract Type","Contract Type"::Contract);
                        if ServContractLine2.FindFirst then begin
                          AnyServItemInOtherContract := true;
                          ServContractLine.Mark(true);
                        end;
                      until ServContractLine.Next = 0;
        #66..68
                    if AnyServItemInOtherContract then
                      if ConfirmManagement.ConfirmProcessUI(
                           StrSubstNo(Text062,Format(xRec.Status),FieldCaption(Status)),true)
                      then begin
                        ServContractLine.MarkedOnly(true);
                        PAGE.RunModal(PAGE::"Service Contract Line List",ServContractLine);
                      end;
                  end;
                Status::Signed:
                  Error(
                    Text009,
                    FieldCaption(Status),Status,FieldCaption("Contract Type"),"Contract Type");
                Status::Canceled:
                  if not ConfirmManagement.ConfirmProcess(StrSubstNo(Text010,"Contract No."),true) then begin
                    Status := xRec.Status;
                    exit;
                  end;
              end;
          end;
          if Status = Status::Canceled then
            "Change Status" := "Change Status"::Locked;
          ServContractLine.Reset;
          ServContractLine.SetRange("Contract Type","Contract Type");
          ServContractLine.SetRange("Contract No.","Contract No.");
          ServContractLine.ModifyAll("Contract Status",Status);
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Customer No."(Field 7).OnValidate". Please convert manually.

        //trigger "(Field 7)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        Cust.GET("Customer No.");
        IF "Customer No." <> xRec."Customer No." THEN BEGIN
          IF ContractLinesExist THEN
            CASE "Contract Type" OF
              "Contract Type"::Contract:
                ERROR(Text011 + Text012,FIELDCAPTION("Customer No."));
              "Contract Type"::Quote:
                ERROR(Text011,FIELDCAPTION("Customer No."));
            END;
          VALIDATE("Ship-to Code",'');
        END;

        "Responsibility Center" := UserMgt.GetRespCenter(2,Cust."Responsibility Center");

        IF "Customer No." <> '' THEN BEGIN
          IF Cust."Bill-to Customer No." = '' THEN BEGIN
            IF "Bill-to Customer No." = "Customer No." THEN
              SkipBillToContact := TRUE;
            VALIDATE("Bill-to Customer No.","Customer No.");
            SkipBillToContact := FALSE;
          END ELSE
            VALIDATE("Bill-to Customer No.",Cust."Bill-to Customer No.");
          IF NOT SkipContact THEN BEGIN
            "Contact Name" := Cust.Contact;
            "Phone No." := Cust."Phone No.";
            "E-Mail" := Cust."E-Mail";
          END;
          "Fax No." := Cust."Fax No.";
        END ELSE BEGIN
          "Contact Name" := '';
          "Phone No." := '';
          "Fax No." := '';
          "E-Mail" := '';
          "Service Zone Code" := '';
        END;

        IF "Customer No." <> xRec."Customer No." THEN BEGIN
          CALCFIELDS(
            Name,"Name 2",Address,"Address 2",
            "Post Code",City,County,"Country/Region Code");
          CALCFIELDS(
            "Bill-to Name","Bill-to Name 2","Bill-to Address","Bill-to Address 2",
            "Bill-to Post Code","Bill-to City","Bill-to County","Bill-to Country/Region Code");
          UpdateShiptoCode;
        END;

        IF NOT SkipContact THEN
          UpdateCont("Customer No.");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        Cust.Get("Customer No.");
        if "Customer No." <> xRec."Customer No." then begin
          if ContractLinesExist then
            case "Contract Type" of
              "Contract Type"::Contract:
                Error(Text011 + Text012,FieldCaption("Customer No."));
              "Contract Type"::Quote:
                Error(Text011,FieldCaption("Customer No."));
            end;
          Validate("Ship-to Code",'');
        end;
        #12..14
        if "Customer No." <> '' then begin
          if Cust."Bill-to Customer No." = '' then begin
            if "Bill-to Customer No." = "Customer No." then
              SkipBillToContact := true;
            Validate("Bill-to Customer No.","Customer No.");
            SkipBillToContact := false;
          end else
            Validate("Bill-to Customer No.",Cust."Bill-to Customer No.");
          if not SkipContact then begin
        #24..26
          end;
          "Fax No." := Cust."Fax No.";
        end else begin
        #30..34
        end;

        if "Customer No." <> xRec."Customer No." then begin
          CalcFields(
            Name,"Name 2",Address,"Address 2",
            "Post Code",City,County,"Country/Region Code");
          CalcFields(
        #42..44
        end;

        if not SkipContact then
          UpdateCont("Customer No.");
        */
        //end;


        //Unsupported feature: CodeModification on ""Salesperson Code"(Field 15).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        ValidateSalesPersonOnServiceContractHeader(Rec,FALSE,FALSE);

        CheckChangeStatus;
        MODIFY;

        CreateDim(
          DATABASE::"Salesperson/Purchaser","Salesperson Code",
          DATABASE::Customer,"Bill-to Customer No.",
          DATABASE::"Responsibility Center","Responsibility Center",
          DATABASE::"Service Contract Template","Template No.",
          DATABASE::"Service Order Type","Service Order Type");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        ValidateSalesPersonOnServiceContractHeader(Rec,false,false);
        CheckChangeStatus;
        Modify;
        #5..11
        */
        //end;


        //Unsupported feature: CodeModification on ""Bill-to Customer No."(Field 16).OnValidate". Please convert manually.

        //trigger "(Field 16)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CheckChangeStatus;
        IF xRec."Bill-to Customer No." <> "Bill-to Customer No." THEN
          IF xRec."Bill-to Customer No." <> '' THEN BEGIN
            IF HideValidationDialog THEN
              Confirmed := TRUE
            ELSE
              Confirmed :=
                ConfirmManagement.ConfirmProcess(
                  STRSUBSTNO(Text014,FIELDCAPTION("Bill-to Customer No.")),TRUE);
          END ELSE
            Confirmed := TRUE;

        IF Confirmed THEN BEGIN
          IF "Bill-to Customer No." <> xRec."Bill-to Customer No." THEN
            IF "Bill-to Customer No." <> '' THEN BEGIN
              Cust.GET("Bill-to Customer No.");
              IF Cust."Privacy Blocked" THEN
                Cust.CustPrivacyBlockedErrorMessage(Cust,FALSE);
              IF Cust.Blocked = Cust.Blocked::All THEN
                Cust.CustBlockedErrorMessage(Cust,FALSE);
            END;

          IF "Customer No." <> '' THEN BEGIN
            Cust.GET("Customer No.");
            IF Cust."Bill-to Customer No." <> '' THEN
              IF "Bill-to Customer No." = '' THEN
                "Bill-to Customer No." := Cust."Bill-to Customer No.";
          END;
          IF "Bill-to Customer No." = '' THEN
            "Bill-to Customer No." := "Customer No.";
          IF Cust.GET("Bill-to Customer No.") THEN BEGIN
            "Currency Code" := Cust."Currency Code";
            "Payment Terms Code" := Cust."Payment Terms Code";
            "Language Code" := Cust."Language Code";
            SetSalespersonCode(Cust."Salesperson Code","Salesperson Code");
            IF NOT SkipBillToContact THEN
              "Bill-to Contact" := Cust.Contact;
          END;

          IF NOT HideValidationDialog THEN
            CustCheckCrLimit.ServiceContractHeaderCheck(Rec);

          CALCFIELDS(
            "Bill-to Name","Bill-to Name 2","Bill-to Address","Bill-to Address 2",
            "Bill-to Post Code","Bill-to City","Bill-to County","Bill-to Country/Region Code");

          IF NOT SkipBillToContact THEN
            UpdateBillToCont("Bill-to Customer No.");
        END ELSE
          "Bill-to Customer No." := xRec."Bill-to Customer No.";

        CreateDim(
          DATABASE::Customer,"Bill-to Customer No.",
          DATABASE::"Salesperson/Purchaser","Salesperson Code",
          DATABASE::"Responsibility Center","Responsibility Center",
          DATABASE::"Service Contract Template","Template No.",
          DATABASE::"Service Order Type","Service Order Type");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        CheckChangeStatus;
        if xRec."Bill-to Customer No." <> "Bill-to Customer No." then
          if xRec."Bill-to Customer No." <> '' then begin
            if HideValidationDialog then
              Confirmed := true
            else
              Confirmed :=
                ConfirmManagement.ConfirmProcess(
                  StrSubstNo(Text014,FieldCaption("Bill-to Customer No.")),true);
          end else
            Confirmed := true;

        if Confirmed then begin
          if "Bill-to Customer No." <> xRec."Bill-to Customer No." then
            if "Bill-to Customer No." <> '' then begin
              Cust.Get("Bill-to Customer No.");
              if Cust."Privacy Blocked" then
                Cust.CustPrivacyBlockedErrorMessage(Cust,false);
              if Cust.Blocked = Cust.Blocked::All then
                Cust.CustBlockedErrorMessage(Cust,false);
            end;

          if "Customer No." <> '' then begin
            Cust.Get("Customer No.");
            if Cust."Bill-to Customer No." <> '' then
              if "Bill-to Customer No." = '' then
                "Bill-to Customer No." := Cust."Bill-to Customer No.";
          end;
          if "Bill-to Customer No." = '' then
            "Bill-to Customer No." := "Customer No.";
          if Cust.Get("Bill-to Customer No.") then begin
        #32..35
            if not SkipBillToContact then
              "Bill-to Contact" := Cust.Contact;
          end;

          if not HideValidationDialog then
            CustCheckCrLimit.ServiceContractHeaderCheck(Rec);

          CalcFields(
        #44..46
          if not SkipBillToContact then
            UpdateBillToCont("Bill-to Customer No.");
        end else
        #50..57
        */
        //end;


        //Unsupported feature: CodeModification on ""Ship-to Code"(Field 22).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF ("Customer No." <> xRec."Customer No.") OR
           ("Ship-to Code" <> xRec."Ship-to Code")
        THEN BEGIN
          IF ContractLinesExist THEN
            ERROR(Text011,FIELDCAPTION("Ship-to Code"));
          UpdateServZone;
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if ("Customer No." <> xRec."Customer No.") or
           ("Ship-to Code" <> xRec."Ship-to Code")
        then begin
          if ContractLinesExist then
            Error(Text011,FieldCaption("Ship-to Code"));
          UpdateServZone;
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Invoice Period"(Field 32).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CalcInvPeriodDuration;
        IF (FORMAT("Price Update Period") <> '') AND
           (CALCDATE("Price Update Period","Starting Date") < CALCDATE(InvPeriodDuration,"Starting Date"))
        THEN
          ERROR(Text065,FIELDCAPTION("Invoice Period"),FIELDCAPTION("Price Update Period"));

        TESTFIELD("Change Status","Change Status"::Open);
        IF ("Invoice Period" = "Invoice Period"::None) AND
           ("Last Invoice Date" <> 0D)
        THEN
          ERROR(Text041,
            FIELDCAPTION("Invoice Period"),
            FORMAT("Invoice Period"),
            TABLECAPTION);

        IF "Invoice Period" = "Invoice Period"::None THEN BEGIN
          "Amount per Period" := 0;
          "Next Invoice Date" := 0D;
          "Next Invoice Period Start" := 0D;
          "Next Invoice Period End" := 0D;
        END ELSE
          IF Prepaid THEN BEGIN
            IF "Next Invoice Date" = 0D THEN BEGIN
              IF "Last Invoice Date" = 0D THEN BEGIN
                TESTFIELD("Starting Date");
                IF "Starting Date" = CALCDATE('<-CM>',"Starting Date") THEN
                  VALIDATE("Next Invoice Date","Starting Date")
                ELSE
                  VALIDATE("Next Invoice Date",CALCDATE('<-CM+1M>',"Starting Date"));
              END ELSE
                IF "Last Invoice Date" = CALCDATE('<-CM>',"Last Invoice Date") THEN
                  VALIDATE("Next Invoice Date",CALCDATE('<CM+1D>',"Last Invoice Period End"))
                ELSE
                  VALIDATE("Next Invoice Date",CALCDATE('<-CM+1M>',"Last Invoice Date"));
            END ELSE
              VALIDATE("Next Invoice Date");
          END ELSE
            VALIDATE("Last Invoice Date");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        CalcInvPeriodDuration;
        if (Format("Price Update Period") <> '') and
           (CalcDate("Price Update Period","Starting Date") < CalcDate(InvPeriodDuration,"Starting Date"))
        then
          Error(Text065,FieldCaption("Invoice Period"),FieldCaption("Price Update Period"));

        TestField("Change Status","Change Status"::Open);
        if ("Invoice Period" = "Invoice Period"::None) and
           ("Last Invoice Date" <> 0D)
        then
          Error(Text041,
            FieldCaption("Invoice Period"),
            Format("Invoice Period"),
            TableCaption);

        if "Invoice Period" = "Invoice Period"::None then begin
        #17..20
        end else
          if Prepaid then begin
            if "Next Invoice Date" = 0D then begin
              if "Last Invoice Date" = 0D then begin
                TestField("Starting Date");
                if "Starting Date" = CalcDate('<-CM>',"Starting Date") then
                  Validate("Next Invoice Date","Starting Date")
                else
                  Validate("Next Invoice Date",CalcDate('<-CM+1M>',"Starting Date"));
              end else
                if "Last Invoice Date" = CalcDate('<-CM>',"Last Invoice Date") then
                  Validate("Next Invoice Date",CalcDate('<CM+1D>',"Last Invoice Period End"))
                else
                  Validate("Next Invoice Date",CalcDate('<-CM+1M>',"Last Invoice Date"));
            end else
              Validate("Next Invoice Date");
          end else
            Validate("Last Invoice Date");
        */
        //end;


        //Unsupported feature: CodeModification on ""Last Invoice Date"(Field 33).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Starting Date");
        IF "Last Invoice Date" = 0D THEN
          IF Prepaid THEN
            TempDate := CALCDATE('<-1D-CM>',"Starting Date")
          ELSE
            TempDate := CALCDATE('<-1D+CM>',"Starting Date")
        ELSE
          TempDate := "Last Invoice Date";
        CASE "Invoice Period" OF
          "Invoice Period"::Month:
            "Next Invoice Date" := CALCDATE('<1M>',TempDate);
          "Invoice Period"::"Two Months":
            "Next Invoice Date" := CALCDATE('<2M>',TempDate);
          "Invoice Period"::Quarter:
            "Next Invoice Date" := CALCDATE('<3M>',TempDate);
          "Invoice Period"::"Half Year":
            "Next Invoice Date" := CALCDATE('<6M>',TempDate);
          "Invoice Period"::Year:
            "Next Invoice Date" := CALCDATE('<12M>',TempDate);
          "Invoice Period"::None:
            IF Prepaid THEN
              "Next Invoice Date" := 0D;
        END;
        IF NOT Prepaid AND ("Next Invoice Date" <> 0D) THEN
          "Next Invoice Date" := CALCDATE('<CM>',"Next Invoice Date");

        IF ("Last Invoice Date" <> 0D) AND ("Last Invoice Date" <> xRec."Last Invoice Date") THEN
          IF Prepaid THEN
            VALIDATE("Last Invoice Period End","Next Invoice Period End")
          ELSE
            VALIDATE("Last Invoice Period End","Last Invoice Date");

        VALIDATE("Next Invoice Date");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Starting Date");
        if "Last Invoice Date" = 0D then
          if Prepaid then
            TempDate := CalcDate('<-1D-CM>',"Starting Date")
          else
            TempDate := CalcDate('<-1D+CM>',"Starting Date")
        else
          TempDate := "Last Invoice Date";
        case "Invoice Period" of
          "Invoice Period"::Month:
            "Next Invoice Date" := CalcDate('<1M>',TempDate);
          "Invoice Period"::"Two Months":
            "Next Invoice Date" := CalcDate('<2M>',TempDate);
          "Invoice Period"::Quarter:
            "Next Invoice Date" := CalcDate('<3M>',TempDate);
          "Invoice Period"::"Half Year":
            "Next Invoice Date" := CalcDate('<6M>',TempDate);
          "Invoice Period"::Year:
            "Next Invoice Date" := CalcDate('<12M>',TempDate);
          "Invoice Period"::None:
            if Prepaid then
              "Next Invoice Date" := 0D;
        end;
        if not Prepaid and ("Next Invoice Date" <> 0D) then
          "Next Invoice Date" := CalcDate('<CM>',"Next Invoice Date");

        if ("Last Invoice Date" <> 0D) and ("Last Invoice Date" <> xRec."Last Invoice Date") then
          if Prepaid then
            Validate("Last Invoice Period End","Next Invoice Period End")
          else
            Validate("Last Invoice Period End","Last Invoice Date");

        Validate("Next Invoice Date");
        */
        //end;


        //Unsupported feature: CodeModification on ""Next Invoice Date"(Field 34).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Next Invoice Date" = 0D THEN BEGIN
          "Next Invoice Period Start" := 0D;
          "Next Invoice Period End" := 0D;
          EXIT;
        END;
        IF "Last Invoice Date" <> 0D THEN
          IF "Last Invoice Date" > "Next Invoice Date" THEN BEGIN
            ServLedgEntry.SETRANGE(Type,ServLedgEntry.Type::"Service Contract");
            ServLedgEntry.SETRANGE("No.","Contract No.");
            IF NOT ServLedgEntry.ISEMPTY THEN
              ERROR(Text023,FIELDCAPTION("Next Invoice Date"),FIELDCAPTION("Last Invoice Date"));
            "Last Invoice Date" := 0D;
          END;

        IF "Next Invoice Date" < "Starting Date" THEN
          ERROR(Text024,FIELDCAPTION("Next Invoice Date"),FIELDCAPTION("Starting Date"));

        IF Prepaid THEN BEGIN
          IF "Next Invoice Date" <> CALCDATE('<-CM>',"Next Invoice Date") THEN BEGIN
            IsHandled := FALSE;
            OnValidateNextInvoiceDateOnBeforeCheck(Rec,IsHandled);
            IF NOT IsHandled THEN
              ERROR(Text026,FIELDCAPTION("Next Invoice Date"));
          END;
          TempDate := CalculateEndPeriodDate(TRUE,"Next Invoice Date");
          IF "Expiration Date" <> 0D THEN
            IF "Next Invoice Date" > "Expiration Date" THEN
              "Next Invoice Date" := 0D
            ELSE
              IF TempDate > "Expiration Date" THEN
                TempDate := "Expiration Date";
          IF ("Next Invoice Date" <> 0D) AND (TempDate <> 0D) THEN BEGIN
            "Next Invoice Period Start" := "Next Invoice Date";
            "Next Invoice Period End" := TempDate;
          END ELSE BEGIN
            "Next Invoice Period Start" := 0D;
            "Next Invoice Period End" := 0D;
          END;
        END ELSE BEGIN
          IF "Next Invoice Date" <> CALCDATE('<CM>',"Next Invoice Date") THEN
            ERROR(Text028,FIELDCAPTION("Next Invoice Date"));
          TempDate := CalculateEndPeriodDate(FALSE,"Next Invoice Date");
          IF TempDate < "Starting Date" THEN
            TempDate := "Starting Date";

          IF "Expiration Date" <> 0D THEN
            IF "Expiration Date" < TempDate THEN
              "Next Invoice Date" := 0D
            ELSE
              IF "Expiration Date" < "Next Invoice Date" THEN
                "Next Invoice Date" := "Expiration Date";

          IF ("Next Invoice Date" <> 0D) AND (TempDate <> 0D) THEN BEGIN
            "Next Invoice Period Start" := TempDate;
            "Next Invoice Period End" := "Next Invoice Date";
          END ELSE BEGIN
            "Next Invoice Period Start" := 0D;
            "Next Invoice Period End" := 0D;
          END;
        END;

        ValidateNextInvoicePeriod;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Next Invoice Date" = 0D then begin
          "Next Invoice Period Start" := 0D;
          "Next Invoice Period End" := 0D;
          exit;
        end;
        if "Last Invoice Date" <> 0D then
          if "Last Invoice Date" > "Next Invoice Date" then begin
            ServLedgEntry.SetRange(Type,ServLedgEntry.Type::"Service Contract");
            ServLedgEntry.SetRange("No.","Contract No.");
            if not ServLedgEntry.IsEmpty then
              Error(Text023,FieldCaption("Next Invoice Date"),FieldCaption("Last Invoice Date"));
            "Last Invoice Date" := 0D;
          end;

        if "Next Invoice Date" < "Starting Date" then
          Error(Text024,FieldCaption("Next Invoice Date"),FieldCaption("Starting Date"));

        if Prepaid then begin
          if "Next Invoice Date" <> CalcDate('<-CM>',"Next Invoice Date") then begin
            IsHandled := false;
            OnValidateNextInvoiceDateOnBeforeCheck(Rec,IsHandled);
            if not IsHandled then
              Error(Text026,FieldCaption("Next Invoice Date"));
          end;
          TempDate := CalculateEndPeriodDate(true,"Next Invoice Date");
          if "Expiration Date" <> 0D then
            if "Next Invoice Date" > "Expiration Date" then
              "Next Invoice Date" := 0D
            else
              if TempDate > "Expiration Date" then
                TempDate := "Expiration Date";
          if ("Next Invoice Date" <> 0D) and (TempDate <> 0D) then begin
            "Next Invoice Period Start" := "Next Invoice Date";
            "Next Invoice Period End" := TempDate;
          end else begin
            "Next Invoice Period Start" := 0D;
            "Next Invoice Period End" := 0D;
          end;
        end else begin
          if "Next Invoice Date" <> CalcDate('<CM>',"Next Invoice Date") then
            Error(Text028,FieldCaption("Next Invoice Date"));
          TempDate := CalculateEndPeriodDate(false,"Next Invoice Date");
          if TempDate < "Starting Date" then
            TempDate := "Starting Date";

          if "Expiration Date" <> 0D then
            if "Expiration Date" < TempDate then
              "Next Invoice Date" := 0D
            else
              if "Expiration Date" < "Next Invoice Date" then
                "Next Invoice Date" := "Expiration Date";

          if ("Next Invoice Date" <> 0D) and (TempDate <> 0D) then begin
            "Next Invoice Period Start" := TempDate;
            "Next Invoice Period End" := "Next Invoice Date";
          end else begin
            "Next Invoice Period Start" := 0D;
            "Next Invoice Period End" := 0D;
          end;
        end;

        ValidateNextInvoicePeriod;
        */
        //end;


        //Unsupported feature: CodeModification on ""Starting Date"(Field 35).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CheckChangeStatus;

        IF "Last Invoice Date" <> 0D THEN
          ERROR(
            Text029,
            FIELDCAPTION("Starting Date"),FORMAT("Contract Type"));
        IF "Starting Date" = 0D THEN BEGIN
          VALIDATE("Next Invoice Date",0D);
          "First Service Date" := 0D;
          ServContractLine.RESET;
          ServContractLine.SETRANGE("Contract Type","Contract Type");
          ServContractLine.SETRANGE("Contract No.","Contract No.");
          ServContractLine.SETRANGE("New Line",TRUE);
          IF ServContractLine.FIND('-') THEN BEGIN
            REPEAT
              ServContractLine."Starting Date" := 0D;
              ServContractLine."Next Planned Service Date" := 0D;
              ServContractLine.MODIFY;
            UNTIL ServContractLine.NEXT = 0;
            MODIFY(TRUE);
          END;
        END ELSE BEGIN
          IF "Starting Date" > "First Service Date" THEN
            "First Service Date" := "Starting Date";
          ServContractLine.RESET;
          ServContractLine.SETRANGE("Contract Type","Contract Type");
          ServContractLine.SETRANGE("Contract No.","Contract No.");
          ServContractLine.SETRANGE("New Line",TRUE);
          IF ServContractLine.FIND('-') THEN BEGIN
            REPEAT
              ServContractLine.SuspendStatusCheck(TRUE);
              ServContractLine."Starting Date" := "Starting Date";
              ServContractLine."Next Planned Service Date" := "First Service Date";
              ServContractLine.MODIFY;
            UNTIL ServContractLine.NEXT = 0;
            MODIFY(TRUE);
          END;
          IF "Next Price Update Date" = 0D THEN
            "Next Price Update Date" := CALCDATE("Price Update Period","Starting Date");
          IF "Invoice Period" <> "Invoice Period"::None THEN
            IF Prepaid THEN BEGIN
              IF "Starting Date" = CALCDATE('<-CM>',"Starting Date") THEN
                VALIDATE("Next Invoice Date","Starting Date")
              ELSE
                VALIDATE("Next Invoice Date",CALCDATE('<-CM+1M>',"Starting Date"))
            END ELSE
              VALIDATE("Last Invoice Date");
          VALIDATE("Service Period");
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        CheckChangeStatus;

        if "Last Invoice Date" <> 0D then
          Error(
            Text029,
            FieldCaption("Starting Date"),Format("Contract Type"));
        if "Starting Date" = 0D then begin
          Validate("Next Invoice Date",0D);
          "First Service Date" := 0D;
          ServContractLine.Reset;
          ServContractLine.SetRange("Contract Type","Contract Type");
          ServContractLine.SetRange("Contract No.","Contract No.");
          ServContractLine.SetRange("New Line",true);
          if ServContractLine.Find('-') then begin
            repeat
              ServContractLine."Starting Date" := 0D;
              ServContractLine."Next Planned Service Date" := 0D;
              ServContractLine.Modify;
            until ServContractLine.Next = 0;
            Modify(true);
          end;
        end else begin
          if "Starting Date" > "First Service Date" then
            "First Service Date" := "Starting Date";
          ServContractLine.Reset;
          ServContractLine.SetRange("Contract Type","Contract Type");
          ServContractLine.SetRange("Contract No.","Contract No.");
          ServContractLine.SetRange("New Line",true);
          if ServContractLine.Find('-') then begin
            repeat
              ServContractLine.SuspendStatusCheck(true);
              ServContractLine."Starting Date" := "Starting Date";
              ServContractLine."Next Planned Service Date" := "First Service Date";
              ServContractLine.Modify;
            until ServContractLine.Next = 0;
            Modify(true);
          end;
          if "Next Price Update Date" = 0D then
            "Next Price Update Date" := CalcDate("Price Update Period","Starting Date");
          if "Invoice Period" <> "Invoice Period"::None then
            if Prepaid then begin
              if "Starting Date" = CalcDate('<-CM>',"Starting Date") then
                Validate("Next Invoice Date","Starting Date")
              else
                Validate("Next Invoice Date",CalcDate('<-CM+1M>',"Starting Date"))
            end else
              Validate("Last Invoice Date");
          Validate("Service Period");
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Expiration Date"(Field 36).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CheckChangeStatus;

        IF "Expiration Date" <> xRec."Expiration Date" THEN BEGIN
          IF "Expiration Date" <> 0D THEN BEGIN
            IF "Expiration Date" < "Starting Date" THEN
              ERROR(Text023,FIELDCAPTION("Expiration Date"),FIELDCAPTION("Starting Date"));
            IF "Last Invoice Date" <> 0D THEN
              IF "Expiration Date" < "Last Invoice Date" THEN
                ERROR(
                  Text023,FIELDCAPTION("Expiration Date"),FIELDCAPTION("Last Invoice Date"));
          END;

          ServContractLine.RESET;
          ServContractLine.SETRANGE("Contract Type","Contract Type");
          ServContractLine.SETRANGE("Contract No.","Contract No.");
          ServContractLine.SETRANGE(Credited,FALSE);

          IF ("Expiration Date" <> 0D) OR
             ("Contract Type" = "Contract Type"::Quote)
          THEN BEGIN
            IF "Contract Type" = "Contract Type"::Contract THEN BEGIN
              ServContractLine.SETFILTER("Contract Expiration Date",'>%1',"Expiration Date");
              IF ServContractLine.FIND('-') THEN
                IF NOT ConfirmManagement.ConfirmProcess(
                     STRSUBSTNO(
                       Text056,FIELDCAPTION("Expiration Date"),
                       TABLECAPTION,"Expiration Date"),TRUE)
                THEN
                  ERROR('');
              ServContractLine.SETFILTER("Contract Expiration Date",'>%1 | %2',"Expiration Date",0D);
            END;

            IF ServContractLine.FIND('-') THEN BEGIN
              REPEAT
                ServContractLine."Contract Expiration Date" := "Expiration Date";
                ServContractLine."Credit Memo Date" := "Expiration Date";
                ServContractLine.MODIFY;
              UNTIL ServContractLine.NEXT = 0;
              MODIFY(TRUE);
            END;
          END;
          VALIDATE("Invoice Period");
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        CheckChangeStatus;

        if "Expiration Date" <> xRec."Expiration Date" then begin
          if "Expiration Date" <> 0D then begin
            if "Expiration Date" < "Starting Date" then
              Error(Text023,FieldCaption("Expiration Date"),FieldCaption("Starting Date"));
            if "Last Invoice Date" <> 0D then
              if "Expiration Date" < "Last Invoice Date" then
                Error(
                  Text023,FieldCaption("Expiration Date"),FieldCaption("Last Invoice Date"));
          end;

          ServContractLine.Reset;
          ServContractLine.SetRange("Contract Type","Contract Type");
          ServContractLine.SetRange("Contract No.","Contract No.");
          ServContractLine.SetRange(Credited,false);

          if ("Expiration Date" <> 0D) or
             ("Contract Type" = "Contract Type"::Quote)
          then begin
            if "Contract Type" = "Contract Type"::Contract then begin
              ServContractLine.SetFilter("Contract Expiration Date",'>%1',"Expiration Date");
              if ServContractLine.Find('-') then
                if not ConfirmManagement.ConfirmProcess(
                     StrSubstNo(
                       Text056,FieldCaption("Expiration Date"),
                       TableCaption,"Expiration Date"),true)
                then
                  Error('');
              ServContractLine.SetFilter("Contract Expiration Date",'>%1 | %2',"Expiration Date",0D);
            end;

            if ServContractLine.Find('-') then begin
              repeat
                ServContractLine."Contract Expiration Date" := "Expiration Date";
                ServContractLine."Credit Memo Date" := "Expiration Date";
                ServContractLine.Modify;
              until ServContractLine.Next = 0;
              Modify(true);
            end;
          end;
          Validate("Invoice Period");
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""First Service Date"(Field 38).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "First Service Date" <> xRec."First Service Date" THEN BEGIN
          IF ("Contract Type" = "Contract Type"::Contract) AND
             (Status = Status::Signed)
          THEN
            ERROR(
              Text030,
              FIELDCAPTION("First Service Date"));

          IF "First Service Date" < "Starting Date" THEN
            ERROR(
              Text023,
              FIELDCAPTION("First Service Date"),
              FIELDCAPTION("Starting Date"));

          IF "Contract Type" = "Contract Type"::Quote THEN BEGIN
            IF ContractLinesExist THEN
              MESSAGE(
                Text031,FIELDCAPTION("First Service Date"));
          END;
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "First Service Date" <> xRec."First Service Date" then begin
          if ("Contract Type" = "Contract Type"::Contract) and
             (Status = Status::Signed)
          then
            Error(
              Text030,
              FieldCaption("First Service Date"));

          if "First Service Date" < "Starting Date" then
            Error(
              Text023,
              FieldCaption("First Service Date"),
              FieldCaption("Starting Date"));

          if "Contract Type" = "Contract Type"::Quote then begin
            if ContractLinesExist then
              Message(
                Text031,FieldCaption("First Service Date"));
          end;
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Annual Amount"(Field 42).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Change Status","Change Status"::Open);
        ServMgtSetup.GET;
        DistributeAmounts;
        VALIDATE("Invoice Period");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Change Status","Change Status"::Open);
        ServMgtSetup.Get;
        DistributeAmounts;
        Validate("Invoice Period");
        */
        //end;


        //Unsupported feature: CodeModification on "Prepaid(Field 45).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF Prepaid <> xRec.Prepaid THEN BEGIN
          IF "Contract Type" = "Contract Type"::Contract THEN BEGIN
            ServLedgEntry.SETCURRENTKEY("Service Contract No.");
            ServLedgEntry.SETRANGE("Service Contract No.","Contract No.");
            IF NOT ServLedgEntry.ISEMPTY THEN
              ERROR(
                Text032,
                FIELDCAPTION(Prepaid),TABLECAPTION,"Contract No.");
          END;
          TESTFIELD("Starting Date");
          IF Prepaid THEN BEGIN
            IF "Invoice after Service" THEN
              ERROR(
                Text057,
                FIELDCAPTION("Invoice after Service"),
                FIELDCAPTION(Prepaid));
            IF "Invoice Period" = "Invoice Period"::None THEN
              VALIDATE("Next Invoice Date",0D)
            ELSE
              IF "Starting Date" = CALCDATE('<-CM>',"Starting Date") THEN
                VALIDATE("Next Invoice Date","Starting Date")
              ELSE
                VALIDATE("Next Invoice Date",CALCDATE('<-CM+1M>',"Starting Date"));
          END ELSE
            VALIDATE("Last Invoice Date");
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if Prepaid <> xRec.Prepaid then begin
          if "Contract Type" = "Contract Type"::Contract then begin
            ServLedgEntry.SetCurrentKey("Service Contract No.");
            ServLedgEntry.SetRange("Service Contract No.","Contract No.");
            if not ServLedgEntry.IsEmpty then
              Error(
                Text032,
                FieldCaption(Prepaid),TableCaption,"Contract No.");
          end;
          TestField("Starting Date");
          if Prepaid then begin
            if "Invoice after Service" then
              Error(
                Text057,
                FieldCaption("Invoice after Service"),
                FieldCaption(Prepaid));
            if "Invoice Period" = "Invoice Period"::None then
              Validate("Next Invoice Date",0D)
            else
              if "Starting Date" = CalcDate('<-CM>',"Starting Date") then
                Validate("Next Invoice Date","Starting Date")
              else
                Validate("Next Invoice Date",CalcDate('<-CM+1M>',"Starting Date"));
          end else
            Validate("Last Invoice Date");
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Next Price Update Date"(Field 52).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Next Price Update Date" < "Next Invoice Date" THEN
          ERROR(Text064,FIELDCAPTION("Next Price Update Date"),FIELDCAPTION("Next Invoice Date"));
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Next Price Update Date" < "Next Invoice Date" then
          Error(Text064,FieldCaption("Next Price Update Date"),FieldCaption("Next Invoice Date"));
        */
        //end;


        //Unsupported feature: CodeModification on ""Response Time (Hours)"(Field 55).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CheckChangeStatus;

        IF "Response Time (Hours)" <> xRec."Response Time (Hours)" THEN BEGIN
          ServContractLine.RESET;
          ServContractLine.SETRANGE("Contract Type","Contract Type");
          ServContractLine.SETRANGE("Contract No.","Contract No.");
          ServContractLine.SETFILTER("Response Time (Hours)",'>%1',"Response Time (Hours)");
          IF ServContractLine.FIND('-') THEN
            IF ConfirmManagement.ConfirmProcess(
                 STRSUBSTNO(Text034,FIELDCAPTION("Response Time (Hours)")),TRUE)
            THEN
              ServContractLine.MODIFYALL("Response Time (Hours)","Response Time (Hours)",TRUE);
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        CheckChangeStatus;

        if "Response Time (Hours)" <> xRec."Response Time (Hours)" then begin
          ServContractLine.Reset;
          ServContractLine.SetRange("Contract Type","Contract Type");
          ServContractLine.SetRange("Contract No.","Contract No.");
          ServContractLine.SetFilter("Response Time (Hours)",'>%1',"Response Time (Hours)");
          if ServContractLine.Find('-') then
            if ConfirmManagement.ConfirmProcess(
                 StrSubstNo(Text034,FieldCaption("Response Time (Hours)")),true)
            then
              ServContractLine.ModifyAll("Response Time (Hours)","Response Time (Hours)",true);
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Service Period"(Field 59).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Service Period" <> xRec."Service Period" THEN BEGIN
          IF ("Contract Type" = "Contract Type"::Contract) AND
             (Status = Status::Signed)
          THEN
            ERROR(
              Text030,
              FIELDCAPTION("Service Period"));
          IF "Contract Type" = "Contract Type"::Quote THEN BEGIN
            IF ContractLinesExist THEN
              MESSAGE(
                Text031,
                FIELDCAPTION("Service Period"));
          END;
          IF ContractLinesExist AND (FORMAT("Service Period") <> '') THEN BEGIN
            ServContractLine.RESET;
            ServContractLine.SETRANGE("Contract Type","Contract Type");
            ServContractLine.SETRANGE("Contract No.","Contract No.");
            IF ServContractLine.FIND('-') THEN
              REPEAT
                IF (FORMAT(ServContractLine."Service Period") = '') OR
                   (ServContractLine."Service Period" = xRec."Service Period")
                THEN BEGIN
                  ServContractLine."Service Period" := "Service Period";
                  ServContractLine.MODIFY;
                END;
              UNTIL ServContractLine.NEXT = 0;
          END;
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Service Period" <> xRec."Service Period" then begin
          if ("Contract Type" = "Contract Type"::Contract) and
             (Status = Status::Signed)
          then
            Error(
              Text030,
              FieldCaption("Service Period"));
          if "Contract Type" = "Contract Type"::Quote then begin
            if ContractLinesExist then
              Message(
                Text031,
                FieldCaption("Service Period"));
          end;
          if ContractLinesExist and (Format("Service Period") <> '') then begin
            ServContractLine.Reset;
            ServContractLine.SetRange("Contract Type","Contract Type");
            ServContractLine.SetRange("Contract No.","Contract No.");
            if ServContractLine.Find('-') then
              repeat
                if (Format(ServContractLine."Service Period") = '') or
                   (ServContractLine."Service Period" = xRec."Service Period")
                then begin
                  ServContractLine."Service Period" := "Service Period";
                  ServContractLine.Modify;
                end;
              until ServContractLine.Next = 0;
          end;
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Invoice after Service"(Field 62).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF NOT ServHeader.READPERMISSION AND
           "Invoice after Service" = TRUE
        THEN
          ERROR(Text054);
        IF "Invoice after Service" AND
           Prepaid
        THEN
          ERROR(
            Text057,
            FIELDCAPTION("Invoice after Service"),
            FIELDCAPTION(Prepaid));
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if not ServHeader.ReadPermission and
           "Invoice after Service" = true
        then
          Error(Text054);
        if "Invoice after Service" and
           Prepaid
        then
          Error(
            Text057,
            FieldCaption("Invoice after Service"),
            FieldCaption(Prepaid));
        */
        //end;


        //Unsupported feature: CodeModification on ""Allow Unbalanced Amounts"(Field 64).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Change Status","Change Status"::Open);
        ServMgtSetup.GET;
        IF "Allow Unbalanced Amounts" <> xRec."Allow Unbalanced Amounts" THEN
          DistributeAmounts;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Change Status","Change Status"::Open);
        ServMgtSetup.Get;
        if "Allow Unbalanced Amounts" <> xRec."Allow Unbalanced Amounts" then
          DistributeAmounts;
        */
        //end;


        //Unsupported feature: CodeModification on ""Shortcut Dimension 1 Code"(Field 67).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CheckChangeStatus;
        ValidateShortcutDimCode(1,"Shortcut Dimension 1 Code");
        MODIFY;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        CheckChangeStatus;
        ValidateShortcutDimCode(1,"Shortcut Dimension 1 Code");
        Modify;
        */
        //end;


        //Unsupported feature: CodeModification on ""Shortcut Dimension 2 Code"(Field 68).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CheckChangeStatus;
        ValidateShortcutDimCode(2,"Shortcut Dimension 2 Code");
        MODIFY;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        CheckChangeStatus;
        ValidateShortcutDimCode(2,"Shortcut Dimension 2 Code");
        Modify;
        */
        //end;


        //Unsupported feature: CodeModification on ""Price Update Period"(Field 75).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CalcInvPeriodDuration;
        IF (FORMAT("Price Update Period") <> '') AND
           (CALCDATE("Price Update Period","Starting Date") < CALCDATE(InvPeriodDuration,"Starting Date"))
        THEN
          ERROR(Text064,FIELDCAPTION("Price Update Period"),FIELDCAPTION("Invoice Period"));

        IF FORMAT("Price Update Period") <> '' THEN
          IF "Last Price Update Date" <> 0D THEN
            "Next Price Update Date" := CALCDATE("Price Update Period","Last Price Update Date")
          ELSE
            "Next Price Update Date" := CALCDATE("Price Update Period","Starting Date")
        ELSE
          "Next Price Update Date" := 0D;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        CalcInvPeriodDuration;
        if (Format("Price Update Period") <> '') and
           (CalcDate("Price Update Period","Starting Date") < CalcDate(InvPeriodDuration,"Starting Date"))
        then
          Error(Text064,FieldCaption("Price Update Period"),FieldCaption("Invoice Period"));

        if Format("Price Update Period") <> '' then
          if "Last Price Update Date" <> 0D then
            "Next Price Update Date" := CalcDate("Price Update Period","Last Price Update Date")
          else
            "Next Price Update Date" := CalcDate("Price Update Period","Starting Date")
        else
          "Next Price Update Date" := 0D;
        */
        //end;


        //Unsupported feature: CodeModification on ""Currency Code"(Field 81).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        MESSAGE(Text042,FIELDCAPTION("Currency Code"));
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        Message(Text042,FieldCaption("Currency Code"));
        */
        //end;


        //Unsupported feature: CodeModification on ""Responsibility Center"(Field 85).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF NOT UserMgt.CheckRespCenter(2,"Responsibility Center") THEN
          ERROR(
            Text040,
            RespCenter.TABLECAPTION,UserMgt.GetSalesFilter);

        CreateDim(
          DATABASE::"Salesperson/Purchaser","Salesperson Code",
          DATABASE::Customer,"Bill-to Customer No.",
          DATABASE::"Responsibility Center","Responsibility Center",
          DATABASE::"Service Contract Template","Template No.",
          DATABASE::"Service Order Type","Service Order Type");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if not UserMgt.CheckRespCenter(2,"Responsibility Center") then
          Error(
            Text040,
            RespCenter.TableCaption,UserMgt.GetSalesFilter);
        #5..11
        */
        //end;


        //Unsupported feature: CodeModification on ""Contact No."(Field 5050).OnLookup". Please convert manually.

        //trigger "(Field 5050)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF ("Customer No." <> '') AND Cont.GET("Contact No.") THEN
          Cont.SETRANGE("Company No.",Cont."Company No.")
        ELSE
          IF "Customer No." <> '' THEN BEGIN
        #5..12
          xRec := Rec;
          VALIDATE("Contact No.",Cont."No.");
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        {IF ("Customer No." <> '') AND Cont.GET("Contact No.") THEN
        #2..15
        }
        */
        //end;


        //Unsupported feature: CodeModification on ""Contact No."(Field 5050).OnValidate". Please convert manually.

        //trigger "(Field 5050)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF ("Contact No." <> xRec."Contact No.") AND (xRec."Contact No." <> '') THEN
          IF NOT ConfirmManagement.ConfirmProcess(
               STRSUBSTNO(Text014,FIELDCAPTION("Contact No.")),TRUE)
          THEN BEGIN
        #5..12
              ERROR(Text045,Cont."No.",Cont.Name,"Customer No.");
        END;

        UpdateCust("Contact No.");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        {IF ("Contact No." <> xRec."Contact No.") AND (xRec."Contact No." <> '') THEN
        #2..15
        UpdateCust("Contact No.");}
        */
        //end;


        //Unsupported feature: CodeModification on ""Bill-to Contact No."(Field 5051).OnLookup". Please convert manually.

        //trigger "(Field 5051)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF ("Bill-to Customer No." <> '') AND Cont.GET("Bill-to Contact No.") THEN
          Cont.SETRANGE("Company No.",Cont."Company No.")
        ELSE
          IF Cust.GET("Bill-to Customer No.") THEN BEGIN
        #5..12
          xRec := Rec;
          VALIDATE("Bill-to Contact No.",Cont."No.");
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        {IF ("Bill-to Customer No." <> '') AND Cont.GET("Bill-to Contact No.") THEN
        #2..15
        }
        */
        //end;


        //Unsupported feature: CodeModification on ""Bill-to Contact No."(Field 5051).OnValidate". Please convert manually.

        //trigger "(Field 5051)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF ("Bill-to Contact No." <> xRec."Bill-to Contact No.") AND
           (xRec."Bill-to Contact No." <> '')
        THEN
          IF NOT ConfirmManagement.ConfirmProcess(
        #5..15
        END;

        UpdateBillToCust("Bill-to Contact No.");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        {IF ("Bill-to Contact No." <> xRec."Bill-to Contact No.") AND
        #2..18
        }
        */
        //end;
        field(16602; "GST Customer Type"; Option)
        {
            CaptionML = ENU = 'GST Customer Type',
                        ENN = 'GST Customer Type';
            DataClassification = ToBeClassified;
            Editable = false;
            OptionCaptionML = ENU = ' ,Registered,Unregistered,Export,Deemed Export,Exempted,SEZ Development,SEZ Unit',
                              ENN = ' ,Registered,Unregistered,Export,Deemed Export,Exempted,SEZ Development,SEZ Unit';
            OptionMembers = " ",Registered,Unregistered,Export,"Deemed Export",Exempted,"SEZ Development","SEZ Unit";
        }
        field(16604; "Invoice Type"; Option)
        {
            CaptionML = ENU = 'Invoice Type',
                        ENN = 'Invoice Type';
            DataClassification = ToBeClassified;
            OptionCaptionML = ENU = ' ,Bill of Supply,Export,Supplementary,Debit Note,Non-GST,Taxable',
                              ENN = ' ,Bill of Supply,Export,Supplementary,Debit Note,Non-GST,Taxable';
            OptionMembers = " ","Bill of Supply",Export,Supplementary,"Debit Note","Non-GST",Taxable;
        }
        field(16608; "GST Bill-to State Code"; Code[10])
        {
            CaptionML = ENU = 'GST Bill-to State Code',
                        ENN = 'GST Bill-to State Code';
            DataClassification = ToBeClassified;
            Editable = false;
            TableRelation = State;
        }
        field(60090; "Dimension Corrected"; Boolean)
        {
            Description = 'added  by sujani for Dimension issue clearance (B2B Assistance)';
            Editable = false;
        }
        field(60091; "OLD Dim Set ID"; Integer)
        {
            Description = 'added  by sujani for Dimension issue clearance (B2B Assistance)';
            Editable = false;
            TableRelation = "Dimension Set Entry Backup2"."Dimension Set ID" WHERE("Dimension Set ID" = FIELD("OLD Dim Set ID"));

            trigger OnLookup();
            begin
                //ShowDimensionsOLD;
            end;
        }
        field(60100; "Period Wise Visits"; DateFormula)
        {
        }
    }
    keys
    {

        //Unsupported feature: Deletion on ""Contract Type,Contract No."(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Contract No.,Contract Type"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Customer No.,Ship-to Code"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Bill-to Customer No.,Contract Type,Combine Invoices,Next Invoice Date"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Next Price Update Date"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Responsibility Center,Service Zone Code,Status,Contract Group Code"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Salesperson Code,Status"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Template No."(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Customer No.,Bill-to Customer No."(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Customer No.,Currency Code,Ship-to Code"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Expiration Date"(Key)". Please convert manually.

        key(Key1; "Contract Type", "Contract No.")
        {
        }
        key(Key2; "Contract No.", "Contract Type")
        {
        }
        key(Key3; "Customer No.", "Ship-to Code")
        {
        }
        key(Key4; "Bill-to Customer No.", "Contract Type", "Combine Invoices", "Next Invoice Date")
        {
        }
        key(Key5; "Next Price Update Date")
        {
        }
        key(Key6; "Responsibility Center", "Service Zone Code", Status, "Contract Group Code")
        {
        }
        key(Key7; "Salesperson Code", Status)
        {
        }
        key(Key8; "Template No.")
        {
        }
        key(Key9; "Customer No.", "Bill-to Customer No.")
        {
            MaintainSQLIndex = false;
        }
        key(Key10; "Customer No.", "Currency Code", "Ship-to Code")
        {
            MaintainSQLIndex = false;
        }
        key(Key11; "Expiration Date")
        {
        }
    }


    //Unsupported feature: CodeModification on "OnDelete". Please convert manually.

    //trigger OnDelete();
    //Parameters and return type have not been exported.
    //>>>> ORIGINAL CODE:
    //begin
    /*
    IF NOT UserMgt.CheckRespCenter(2,"Responsibility Center") THEN
      ERROR(
        Text002,
        RespCenter.TABLECAPTION,UserMgt.GetSalesFilter);

    IF "Contract Type" = "Contract Type"::Contract THEN BEGIN
      MoveEntries.MoveServContractLedgerEntries(Rec);

      IF Status = Status::Signed THEN
        ERROR(Text003,FORMAT(Status),TABLECAPTION);

      ServLedgEntry.SETRANGE(Type,ServLedgEntry.Type::"Service Contract");
      ServLedgEntry.SETRANGE("No.","Contract No.");
      ServLedgEntry.SETRANGE(Prepaid,FALSE);
      ServLedgEntry.SETRANGE(Open,TRUE);
      IF NOT ServLedgEntry.ISEMPTY THEN
        IF NOT ConfirmManagement.ConfirmProcess(
             STRSUBSTNO(Text052,ServLedgEntry.FIELDCAPTION(Open)),TRUE)
        THEN
          ERROR(Text053);
    END;
    ServContractLine.RESET;
    ServContractLine.SETRANGE("Contract Type","Contract Type");
    ServContractLine.SETRANGE("Contract No.","Contract No.");
    ServContractLine.DELETEALL;

    ServCommentLine.SETRANGE("Table Name",ServCommentLine."Table Name"::"Service Contract");
    ServCommentLine.SETRANGE("Table Subtype","Contract Type");
    ServCommentLine.SETRANGE("No.","Contract No.");
    ServCommentLine.DELETEALL;

    ServHour.RESET;
    CASE "Contract Type" OF
      "Contract Type"::Quote:
        ServHour.SETRANGE("Service Contract Type",ServHour."Service Contract Type"::Quote);
      "Contract Type"::Contract:
        ServHour.SETRANGE("Service Contract Type",ServHour."Service Contract Type"::Contract);
    END;
    ServHour.SETRANGE("Service Contract No.","Contract No.");
    ServHour.DELETEALL;

    FiledServContract.RESET;
    FiledServContract.SETCURRENTKEY("Contract Type Relation","Contract No. Relation");
    FiledServContract.SETRANGE("Contract Type Relation","Contract Type");
    FiledServContract.SETRANGE("Contract No. Relation","Contract No.");
    FiledServContract.DELETEALL(TRUE);
    */
    //end;
    //>>>> MODIFIED CODE:
    //begin
    /*
    if not UserMgt.CheckRespCenter(2,"Responsibility Center") then
      Error(
        Text002,
        RespCenter.TableCaption,UserMgt.GetSalesFilter);

    if "Contract Type" = "Contract Type"::Contract then begin
      MoveEntries.MoveServContractLedgerEntries(Rec);

      if Status = Status::Signed then
        Error(Text003,Format(Status),TableCaption);

      ServLedgEntry.SetRange(Type,ServLedgEntry.Type::"Service Contract");
      ServLedgEntry.SetRange("No.","Contract No.");
      ServLedgEntry.SetRange(Prepaid,false);
      ServLedgEntry.SetRange(Open,true);
      if not ServLedgEntry.IsEmpty then
        if not ConfirmManagement.ConfirmProcess(
             StrSubstNo(Text052,ServLedgEntry.FieldCaption(Open)),true)
        then
          Error(Text053);
    end;
    ServContractLine.Reset;
    ServContractLine.SetRange("Contract Type","Contract Type");
    ServContractLine.SetRange("Contract No.","Contract No.");
    ServContractLine.DeleteAll;

    ServCommentLine.SetRange("Table Name",ServCommentLine."Table Name"::"Service Contract");
    ServCommentLine.SetRange("Table Subtype","Contract Type");
    ServCommentLine.SetRange("No.","Contract No.");
    ServCommentLine.DeleteAll;

    ServHour.Reset;
    case "Contract Type" of
      "Contract Type"::Quote:
        ServHour.SetRange("Service Contract Type",ServHour."Service Contract Type"::Quote);
      "Contract Type"::Contract:
        ServHour.SetRange("Service Contract Type",ServHour."Service Contract Type"::Contract);
    end;
    ServHour.SetRange("Service Contract No.","Contract No.");
    ServHour.DeleteAll;

    FiledServContract.Reset;
    FiledServContract.SetCurrentKey("Contract Type Relation","Contract No. Relation");
    FiledServContract.SetRange("Contract Type Relation","Contract Type");
    FiledServContract.SetRange("Contract No. Relation","Contract No.");
    FiledServContract.DeleteAll(true);
    */
    //end;


    //Unsupported feature: CodeModification on "OnInsert". Please convert manually.

    //trigger OnInsert();
    //Parameters and return type have not been exported.
    //>>>> ORIGINAL CODE:
    //begin
    /*
    ServMgtSetup.GET;
    IF "Contract No." = '' THEN BEGIN
      ServMgtSetup.TESTFIELD("Service Contract Nos.");
      NoSeriesMgt.InitSeries(ServMgtSetup."Service Contract Nos.",xRec."No. Series",0D,
        "Contract No.","No. Series");
    END;
    "Starting Date" := WORKDATE;
    "First Service Date" := WORKDATE;

    IsHandled := FALSE;
    OnBeforeApplyServiceContractQuoteTemplate(Rec,IsHandled);
    IF NOT IsHandled THEN BEGIN
      ServiceContractTemplate.RESET;
      IF ServiceContractTemplate.FINDFIRST THEN
        IF ConfirmManagement.ConfirmProcess(Text000,FALSE) THEN BEGIN
          COMMIT;
          CLEAR(ServContractQuoteTmplUpd);
          ServContractQuoteTmplUpd.RUN(Rec);
        END;
    END;

    VALIDATE("Starting Date");
    */
    //end;
    //>>>> MODIFIED CODE:
    //begin
    /*
    ServMgtSetup.Get;
    if "Contract No." = '' then begin
      ServMgtSetup.TestField("Service Contract Nos.");
      NoSeriesMgt.InitSeries(ServMgtSetup."Service Contract Nos.",xRec."No. Series",0D,
        "Contract No.","No. Series");
    end;
    "Starting Date" := WorkDate;
    "First Service Date" := WorkDate;

    IsHandled := false;
    OnBeforeApplyServiceContractQuoteTemplate(Rec,IsHandled);
    if not IsHandled then begin
      ServiceContractTemplate.Reset;
      if ServiceContractTemplate.FindFirst then
        if ConfirmManagement.ConfirmProcess(Text000,false) then begin
          Commit;
          Clear(ServContractQuoteTmplUpd);
          ServContractQuoteTmplUpd.Run(Rec);
        end;
    end;

    Validate("Starting Date");
    */
    //end;


    //Unsupported feature: CodeModification on "OnModify". Please convert manually.

    //trigger OnModify();
    //Parameters and return type have not been exported.
    //>>>> ORIGINAL CODE:
    //begin
    /*
    CheckChangeStatus;
    IF ("Contract Type" = "Contract Type"::Contract) AND ("Contract No." <> '') THEN BEGIN
      ServMgtSetup.GET;
      IF ServMgtSetup."Register Contract Changes" THEN
        UpdContractChangeLog(xRec);

      IF (Status <> xRec.Status) AND
         (Status = Status::Canceled)
      THEN
        ContractGainLossEntry.AddEntry(3,"Contract Type","Contract No.",-"Annual Amount","Cancel Reason Code");
    END;

    IF (Status = Status::Signed) AND
       ("Annual Amount" <> xRec."Annual Amount")
    THEN
      ContractGainLossEntry.AddEntry(4,"Contract Type",
        "Contract No.","Annual Amount" - xRec."Annual Amount",'');
    */
    //end;
    //>>>> MODIFIED CODE:
    //begin
    /*
    CheckChangeStatus;
    if ("Contract Type" = "Contract Type"::Contract) and ("Contract No." <> '') then begin
      ServMgtSetup.Get;
      if ServMgtSetup."Register Contract Changes" then
        UpdContractChangeLog(xRec);

      if (Status <> xRec.Status) and
         (Status = Status::Canceled)
      then
        ContractGainLossEntry.AddEntry(3,"Contract Type","Contract No.",-"Annual Amount","Cancel Reason Code");
    end;

    if (Status = Status::Signed) and
       ("Annual Amount" <> xRec."Annual Amount")
    then
      ContractGainLossEntry.AddEntry(4,"Contract Type",
        "Contract No.","Annual Amount" - xRec."Annual Amount",'');
    */
    //end;


    //Unsupported feature: CodeModification on "OnRename". Please convert manually.

    //trigger OnRename();
    //Parameters and return type have not been exported.
    //>>>> ORIGINAL CODE:
    //begin
    /*
    ERROR(Text063,TABLECAPTION);
    */
    //end;
    //>>>> MODIFIED CODE:
    //begin
    /*
    Error(Text063,TableCaption);
    */
    //end;

    //Unsupported feature: PropertyChange. Please convert manually.


    //Unsupported feature: PropertyChange. Please convert manually.


    //Unsupported feature: PropertyChange. Please convert manually.


    //Unsupported feature: PropertyChange. Please convert manually.



    //Unsupported feature: PropertyModification on "Status(Field 5).OnValidate.ServLedgEntry(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Status : 5907;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Status : "Service Ledger Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "Status(Field 5).OnValidate.ConfirmManagement(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Status : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Status : "Confirm Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bill-to Customer No."(Field 16).OnValidate.CustCheckCrLimit(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bill-to Customer No." : 312;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bill-to Customer No." : "Cust-Check Cr. Limit";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bill-to Customer No."(Field 16).OnValidate.ConfirmManagement(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bill-to Customer No." : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bill-to Customer No." : "Confirm Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Next Invoice Date"(Field 34).OnValidate.ServLedgEntry(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Next Invoice Date" : 5907;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Next Invoice Date" : "Service Ledger Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Expiration Date"(Field 36).OnValidate.ConfirmManagement(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Expiration Date" : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Expiration Date" : "Confirm Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "Prepaid(Field 45).OnValidate.ServLedgEntry(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Prepaid : 5907;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Prepaid : "Service Ledger Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Response Time (Hours)"(Field 55).OnValidate.ConfirmManagement(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Response Time  : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Response Time  : "Confirm Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""E-Mail"(Field 88).OnValidate.MailManagement(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"E-Mail" : 9520;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"E-Mail" : "Mail Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Contact No."(Field 5050).OnLookup.Cont(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Contact No." : 5050;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Contact No." : Contact;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Contact No."(Field 5050).OnLookup.ContBusinessRelation(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Contact No." : 5054;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Contact No." : "Contact Business Relation";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Contact No."(Field 5050).OnValidate.Cont(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Contact No." : 5050;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Contact No." : Contact;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Contact No."(Field 5050).OnValidate.ContBusinessRelation(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Contact No." : 5054;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Contact No." : "Contact Business Relation";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Contact No."(Field 5050).OnValidate.ConfirmManagement(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Contact No." : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Contact No." : "Confirm Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bill-to Contact No."(Field 5051).OnLookup.Cont(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bill-to Contact No." : 5050;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bill-to Contact No." : Contact;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bill-to Contact No."(Field 5051).OnLookup.ContBusinessRelation(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bill-to Contact No." : 5054;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bill-to Contact No." : "Contact Business Relation";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bill-to Contact No."(Field 5051).OnValidate.Cont(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bill-to Contact No." : 5050;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bill-to Contact No." : Contact;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bill-to Contact No."(Field 5051).OnValidate.ContBusinessRelation(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bill-to Contact No." : 5054;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bill-to Contact No." : "Contact Business Relation";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bill-to Contact No."(Field 5051).OnValidate.ConfirmManagement(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bill-to Contact No." : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bill-to Contact No." : "Confirm Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "OnDelete.ServLedgEntry(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //OnDelete.ServLedgEntry : 5907;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //OnDelete.ServLedgEntry : "Service Ledger Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "OnDelete.ConfirmManagement(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //OnDelete.ConfirmManagement : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //OnDelete.ConfirmManagement : "Confirm Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "OnInsert.ServiceContractTemplate(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //OnInsert.ServiceContractTemplate : 5968;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //OnInsert.ServiceContractTemplate : "Service Contract Template";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "OnInsert.ServContractQuoteTmplUpd(Variable 1003)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //OnInsert.ServContractQuoteTmplUpd : 5942;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //OnInsert.ServContractQuoteTmplUpd : "ServContractQuote-Tmpl. Upd.";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "OnInsert.ConfirmManagement(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //OnInsert.ConfirmManagement : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //OnInsert.ConfirmManagement : "Confirm Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CreateDim(PROCEDURE 16).SourceCodeSetup(Variable 1010)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CreateDim : 242;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CreateDim : "Source Code Setup";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateCont(PROCEDURE 24).ContBusRel(Variable 1003)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateCont : 5054;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateCont : "Contact Business Relation";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateCont(PROCEDURE 24).Cont(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateCont : 5050;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateCont : Contact;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateCont(PROCEDURE 24).Cust(Variable 1004)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateCont : 18;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateCont : Customer;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateBillToCont(PROCEDURE 27).ContBusRel(Variable 1003)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateBillToCont : 5054;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateBillToCont : "Contact Business Relation";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateBillToCont(PROCEDURE 27).Cont(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateBillToCont : 5050;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateBillToCont : Contact;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateBillToCont(PROCEDURE 27).Cust(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateBillToCont : 18;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateBillToCont : Customer;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateCust(PROCEDURE 25).ContBusinessRelation(Variable 1007)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateCust : 5054;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateCust : "Contact Business Relation";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateCust(PROCEDURE 25).Cust(Variable 1006)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateCust : 18;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateCust : Customer;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateCust(PROCEDURE 25).Cont(Variable 1005)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateCust : 5050;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateCust : Contact;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateBillToCust(PROCEDURE 26).ContBusinessRelation(Variable 1005)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateBillToCust : 5054;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateBillToCust : "Contact Business Relation";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateBillToCust(PROCEDURE 26).Cust(Variable 1004)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateBillToCust : 18;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateBillToCust : Customer;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UpdateBillToCust(PROCEDURE 26).Cont(Variable 1003)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UpdateBillToCust : 5050;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UpdateBillToCust : Contact;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "EvenDistribution(PROCEDURE 12).OldServContractLine(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //EvenDistribution : 5964;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //EvenDistribution : "Service Contract Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ProfitBasedDistribution(PROCEDURE 13).OldServContractLine(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ProfitBasedDistribution : 5964;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ProfitBasedDistribution : "Service Contract Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "AmountBasedDistribution(PROCEDURE 14).OldServContractLine(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //AmountBasedDistribution : 5964;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //AmountBasedDistribution : "Service Contract Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "DistributeAmounts(PROCEDURE 17).OldServContractLine(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //DistributeAmounts : 5964;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //DistributeAmounts : "Service Contract Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "DistributeAmounts(PROCEDURE 17).ContractAmountDistribution(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //DistributeAmounts : 6079;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //DistributeAmounts : "Contract Amount Distribution";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServHeader(Variable 1075)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServHeader : 5900;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServHeader : "Service Header";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServContractHeader(Variable 1032)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServContractHeader : 5965;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServContractHeader : "Service Contract Header";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServContractLine(Variable 1059)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServContractLine : 5964;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServContractLine : "Service Contract Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServMgtSetup(Variable 1034)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServMgtSetup : 5911;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServMgtSetup : "Service Mgt. Setup";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServCommentLine(Variable 1035)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServCommentLine : 5906;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServCommentLine : "Service Comment Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "Cust(Variable 1036)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Cust : 18;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Cust : Customer;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ShipToAddr(Variable 1037)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ShipToAddr : 222;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ShipToAddr : "Ship-to Address";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ContractChangeLog(Variable 1038)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ContractChangeLog : 5967;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ContractChangeLog : "Contract Change Log";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "FiledServContract(Variable 1039)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //FiledServContract : 5970;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //FiledServContract : "Filed Service Contract Header";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ContractGainLossEntry(Variable 1042)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ContractGainLossEntry : 5969;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ContractGainLossEntry : "Contract Gain/Loss Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "RespCenter(Variable 1044)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //RespCenter : 5714;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //RespCenter : "Responsibility Center";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServHour(Variable 1069)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServHour : 5910;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServHour : "Service Hour";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServContractLine2(Variable 1031)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServContractLine2 : 5964;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServContractLine2 : "Service Contract Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "Currency(Variable 1061)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Currency : 4;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Currency : Currency;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "Salesperson(Variable 1913)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Salesperson : 13;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Salesperson : "Salesperson/Purchaser";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "NoSeriesMgt(Variable 1045)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //NoSeriesMgt : 396;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //NoSeriesMgt : NoSeriesManagement;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UserMgt(Variable 1046)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UserMgt : 5700;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UserMgt : "User Setup Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServContractMgt(Variable 1047)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServContractMgt : 5940;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServContractMgt : ServContractManagement;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ServOrderMgt(Variable 1048)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ServOrderMgt : 5900;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ServOrderMgt : ServOrderManagement;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "DimMgt(Variable 1040)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //DimMgt : 408;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //DimMgt : DimensionManagement;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "MoveEntries(Variable 1077)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //MoveEntries : 361;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //MoveEntries : MoveEntries;
    //Variable type has not been exported.
}


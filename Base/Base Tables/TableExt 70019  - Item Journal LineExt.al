tableextension 70019 ItemJournalLineExt extends "Item Journal Line"
{
    // version NAVW19.00.00.51552,NAVIN9.00.00.51552,QC1.0,B2B1.0,RQC1.0,Rev01,EFFUPG

    fields
    {

        //Unsupported feature: Change TableRelation on ""Item No."(Field 3)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Entry Type"(Field 5)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Source No."(Field 6)". Please convert manually.


        //Unsupported feature: Change Editable on ""Source No."(Field 6)". Please convert manually.


        //Unsupported feature: Change Data type on "Description(Field 8)". Please convert manually.


        //Unsupported feature: Change Editable on ""Inventory Posting Group"(Field 10)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Source Posting Group"(Field 11)". Please convert manually.


        //Unsupported feature: Change Editable on ""Source Posting Group"(Field 11)". Please convert manually.


        //Unsupported feature: Change Editable on ""Invoiced Quantity"(Field 15)". Please convert manually.


        //Unsupported feature: Change Editable on ""Discount Amount"(Field 22)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Salespers./Purch. Code"(Field 23)". Please convert manually.


        //Unsupported feature: Change Editable on ""Source Code"(Field 26)". Please convert manually.


        //Unsupported feature: Change Editable on ""Item Shpt. Entry No."(Field 32)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Shortcut Dimension 1 Code"(Field 34)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Shortcut Dimension 2 Code"(Field 35)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Source Type"(Field 39)". Please convert manually.


        //Unsupported feature: Change Editable on ""Source Type"(Field 39)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Journal Batch Name"(Field 41)". Please convert manually.


        //Unsupported feature: Change BlankZero on ""Recurring Method"(Field 43)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Drop Shipment"(Field 46)". Please convert manually.


        //Unsupported feature: Change Editable on ""Drop Shipment"(Field 46)". Please convert manually.


        //Unsupported feature: Change Data type on ""Transport Method"(Field 48)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Country/Region Code"(Field 49)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""New Shortcut Dimension 1 Code"(Field 51)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""New Shortcut Dimension 2 Code"(Field 52)". Please convert manually.


        //Unsupported feature: Change Editable on ""Qty. (Calculated)"(Field 53)". Please convert manually.


        //Unsupported feature: Change TestTableRelation on ""Last Item Ledger Entry No."(Field 55)". Please convert manually.


        //Unsupported feature: Change Editable on ""Last Item Ledger Entry No."(Field 55)". Please convert manually.


        //Unsupported feature: Change Editable on ""Phys. Inventory"(Field 56)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Reserved Quantity"(Field 68)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Reserved Quantity"(Field 68)". Please convert manually.


        //Unsupported feature: Change Editable on ""Reserved Quantity"(Field 68)". Please convert manually.


        //Unsupported feature: Change Editable on ""Unit Cost (ACY)"(Field 72)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Source Currency Code"(Field 73)". Please convert manually.


        //Unsupported feature: Change Editable on ""Source Currency Code"(Field 73)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Document Type"(Field 79)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Order Type"(Field 90)". Please convert manually.


        //Unsupported feature: Change Editable on ""Order Type"(Field 90)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Order No."(Field 91)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Order Line No."(Field 92)". Please convert manually.


        //Unsupported feature: Change Editable on ""Dimension Set ID"(Field 480)". Please convert manually.


        //Unsupported feature: Change Editable on ""New Dimension Set ID"(Field 481)". Please convert manually.


        //Unsupported feature: Change Editable on ""Assemble to Order"(Field 904)". Please convert manually.


        //Unsupported feature: Change Editable on ""Job Contract Entry No."(Field 1030)". Please convert manually.


        //Unsupported feature: Change Data type on ""Variant Code"(Field 5402)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Variant Code"(Field 5402)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Bin Code"(Field 5403)". Please convert manually.


        //Unsupported feature: Change Editable on ""Qty. per Unit of Measure"(Field 5404)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""New Bin Code"(Field 5406)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Unit of Measure Code"(Field 5407)". Please convert manually.


        //Unsupported feature: Change Editable on ""Derived from Blanket Order"(Field 5408)". Please convert manually.


        //Unsupported feature: Change Editable on ""Invoiced Qty. (Base)"(Field 5415)". Please convert manually.


        //Unsupported feature: Change CalcFormula on ""Reserved Qty. (Base)"(Field 5468)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Reserved Qty. (Base)"(Field 5468)". Please convert manually.


        //Unsupported feature: Change Editable on ""Reserved Qty. (Base)"(Field 5468)". Please convert manually.


        //Unsupported feature: Change Editable on "Level(Field 5560)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Flushing Method"(Field 5561)". Please convert manually.


        //Unsupported feature: Change Editable on ""Flushing Method"(Field 5561)". Please convert manually.


        //Unsupported feature: Change Editable on ""Changed by User"(Field 5562)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Originally Ordered No."(Field 5701)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Originally Ordered Var. Code"(Field 5702)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Originally Ordered Var. Code"(Field 5702)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Purchasing Code"(Field 5706)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Product Group Code"(Field 5707)". Please convert manually.


        //Unsupported feature: Change ValidateTableRelation on ""Product Group Code"(Field 5707)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Value Entry Type"(Field 5800)". Please convert manually.


        //Unsupported feature: Change Editable on ""Inventory Value (Calculated)"(Field 5802)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Variance Type"(Field 5804)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Inventory Value Per"(Field 5805)". Please convert manually.


        //Unsupported feature: Change Editable on ""Inventory Value Per"(Field 5805)". Please convert manually.


        //Unsupported feature: Change Editable on ""Partial Revaluation"(Field 5806)". Please convert manually.


        //Unsupported feature: Change Editable on ""Unit Cost (Calculated)"(Field 5809)". Please convert manually.


        //Unsupported feature: Change Editable on ""Applied Amount"(Field 5811)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Invoice-to Source No."(Field 5820)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on "Type(Field 5830)". Please convert manually.


        //Unsupported feature: Change OptionString on "Type(Field 5830)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""No."(Field 5831)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Operation No."(Field 5838)". Please convert manually.


        //Unsupported feature: Change Editable on ""Work Center No."(Field 5839)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Setup Time"(Field 5841)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Run Time"(Field 5842)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Stop Time"(Field 5843)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Output Quantity"(Field 5846)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Scrap Quantity"(Field 5847)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Concurrent Capacity"(Field 5849)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Cap. Unit of Measure Code"(Field 5858)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Starting Time"(Field 5873)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on ""Ending Time"(Field 5874)". Please convert manually.


        //Unsupported feature: Change Editable on ""Routing No."(Field 5882)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Prod. Order Comp. Line No."(Field 5884)". Please convert manually.


        //Unsupported feature: Change AccessByPermission on "Finished(Field 5885)". Please convert manually.


        //Unsupported feature: Change Data type on ""Stop Code"(Field 5895)". Please convert manually.


        //Unsupported feature: Change TableRelation on ""Stop Code"(Field 5895)". Please convert manually.


        //Unsupported feature: Change Editable on ""Work Center Group Code"(Field 5898)". Please convert manually.


        //Unsupported feature: Change Editable on ""Serial No."(Field 6500)". Please convert manually.


        //Unsupported feature: Change Editable on ""Lot No."(Field 6501)". Please convert manually.


        //Unsupported feature: Change Editable on ""Warranty Date"(Field 6502)". Please convert manually.


        //Unsupported feature: Change Editable on ""New Serial No."(Field 6503)". Please convert manually.


        //Unsupported feature: Change Editable on ""New Lot No."(Field 6504)". Please convert manually.


        //Unsupported feature: Change Editable on ""Item Expiration Date"(Field 6506)". Please convert manually.


        //Unsupported feature: Change Editable on ""Phys Invt Counting Period Code"(Field 7380)". Please convert manually.


        //Unsupported feature: Change OptionString on ""Phys Invt Counting Period Type"(Field 7381)". Please convert manually.


        //Unsupported feature: Change Editable on ""Phys Invt Counting Period Type"(Field 7381)". Please convert manually.


        //Unsupported feature: CodeModification on ""Item No."(Field 3).OnValidate". Please convert manually.

        //trigger "(Field 3)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Item No." <> xRec."Item No." THEN BEGIN
          "Variant Code" := '';
          "Bin Code" := '';
          IF CurrFieldNo <> 0 THEN
            WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION("Item No."));
          IF ("Location Code" <> '') AND ("Item No." <> '') THEN BEGIN
            GetLocation("Location Code");
            IF Location."Bin Mandatory" AND NOT Location."Directed Put-away and Pick" THEN
              WMSManagement.GetDefaultBin("Item No.","Variant Code","Location Code","Bin Code")
          END;
          IF ("Entry Type" = "Entry Type"::Transfer) AND ("Location Code" = "New Location Code") THEN
            "New Bin Code" := "Bin Code";
        END;

        IF "Entry Type" IN ["Entry Type"::Consumption,"Entry Type"::Output] THEN
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        IF "Item No." = '' THEN BEGIN
          CreateDim(
            DATABASE::Item,"Item No.",
            DATABASE::"Salesperson/Purchaser","Salespers./Purch. Code",
            DATABASE::"Work Center","Work Center No.");
          EXIT;
        END;

        GetItem;
        OnValidateItemNoOnAfterGetItem(Rec,Item);
        DisplayErrorIfItemIsBlocked(Item);
        ValidateTypeWithItemNo;

        IF "Value Entry Type" = "Value Entry Type"::Revaluation THEN
          Item.TESTFIELD("Inventory Value Zero",FALSE);
        Description := Item.Description;
        "Inventory Posting Group" := Item."Inventory Posting Group";
        "Item Category Code" := Item."Item Category Code";

        IF ("Value Entry Type" <> "Value Entry Type"::"Direct Cost") OR
           ("Item Charge No." <> '')
        THEN BEGIN
          IF "Item No." <> xRec."Item No." THEN BEGIN
            TESTFIELD("Partial Revaluation",FALSE);
            RetrieveCosts;
            "Indirect Cost %" := 0;
            "Overhead Rate" := 0;
            "Inventory Value Per" := "Inventory Value Per"::" ";
            VALIDATE("Applies-to Entry",0);
            "Partial Revaluation" := FALSE;
          END;
        END ELSE BEGIN
          "Indirect Cost %" := Item."Indirect Cost %";
          "Overhead Rate" := Item."Overhead Rate";
          IF NOT "Phys. Inventory" OR (Item."Costing Method" = Item."Costing Method"::Standard) THEN BEGIN
            RetrieveCosts;
            "Unit Cost" := UnitCost;
          END ELSE
            UnitCost := "Unit Cost";
        END;

        IF (("Entry Type" = "Entry Type"::Output) AND (WorkCenter."No." = '') AND (MachineCenter."No." = '')) OR
           ("Entry Type" <> "Entry Type"::Output) OR
           ("Value Entry Type" = "Value Entry Type"::Revaluation)
        THEN
          "Gen. Prod. Posting Group" := Item."Gen. Prod. Posting Group";

        CASE "Entry Type" OF
          "Entry Type"::Purchase,
          "Entry Type"::Output,
          "Entry Type"::"Assembly Output":
            PurchPriceCalcMgt.FindItemJnlLinePrice(Rec,FIELDNO("Item No."));
          "Entry Type"::"Positive Adjmt.",
          "Entry Type"::"Negative Adjmt.",
          "Entry Type"::Consumption,
          "Entry Type"::"Assembly Consumption":
            "Unit Amount" := UnitCost;
          "Entry Type"::Sale:
            SalesPriceCalcMgt.FindItemJnlLinePrice(Rec,FIELDNO("Item No."));
          "Entry Type"::Transfer:
            BEGIN
              "Unit Amount" := 0;
              "Unit Cost" := 0;
              Amount := 0;
            END;
        END;

        CASE "Entry Type" OF
          "Entry Type"::Purchase:
            "Unit of Measure Code" := Item."Purch. Unit of Measure";
          "Entry Type"::Sale:
            "Unit of Measure Code" := Item."Sales Unit of Measure";
          "Entry Type"::Output:
            BEGIN
              Item.TESTFIELD("Inventory Value Zero",FALSE);
              ProdOrderLine.SetFilterByReleasedOrderNo("Order No.");
              ProdOrderLine.SETRANGE("Item No.","Item No.");
              IF ProdOrderLine.FINDFIRST THEN BEGIN
                "Routing No." := ProdOrderLine."Routing No.";
                "Source Type" := "Source Type"::Item;
                "Source No." := ProdOrderLine."Item No.";
              END ELSE
                IF ("Value Entry Type" <> "Value Entry Type"::Revaluation) AND
                   (CurrFieldNo <> 0)
                THEN
                  ERROR(Text031,"Item No.","Order No.");
              IF ProdOrderLine.COUNT = 1 THEN
                CopyFromProdOrderLine(ProdOrderLine)
              ELSE
                "Unit of Measure Code" := Item."Base Unit of Measure";
            END;
          "Entry Type"::Consumption:
            IF FindProdOrderComponent(ProdOrderComp) THEN
              CopyFromProdOrderComp(ProdOrderComp)
            ELSE BEGIN
              "Unit of Measure Code" := Item."Base Unit of Measure";
              VALIDATE("Prod. Order Comp. Line No.",0);
            END;
        END;

        IF "Unit of Measure Code" = '' THEN
          "Unit of Measure Code" := Item."Base Unit of Measure";

        IF "Value Entry Type" = "Value Entry Type"::Revaluation THEN
          "Unit of Measure Code" := Item."Base Unit of Measure";
        VALIDATE("Unit of Measure Code");
        IF "Variant Code" <> '' THEN
          VALIDATE("Variant Code");

        OnAfterOnValidateItemNoAssignByEntryType(Rec,Item);

        CheckItemAvailable(FIELDNO("Item No."));

        IF ((NOT ("Order Type" IN ["Order Type"::Production,"Order Type"::Assembly])) OR ("Order No." = '')) AND NOT "Phys. Inventory"
        THEN
          CreateDim(
            DATABASE::Item,"Item No.",
            DATABASE::"Salesperson/Purchaser","Salespers./Purch. Code",
            DATABASE::"Work Center","Work Center No.");

        OnBeforeVerifyReservedQty(Rec,xRec,FIELDNO("Item No."));
        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        */
        //end;


        //Unsupported feature: CodeModification on ""Posting Date"(Field 4).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Posting Date");
        VALIDATE("Document Date","Posting Date");
        CheckDateConflict.ItemJnlLineCheck(Rec,CurrFieldNo <> 0);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Posting Date");
        Validate("Document Date","Posting Date");
        CheckDateConflict.ItemJnlLineCheck(Rec,CurrFieldNo <> 0);
        */
        //end;


        //Unsupported feature: CodeModification on ""Entry Type"(Field 5).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF NOT ("Entry Type" IN ["Entry Type"::"Positive Adjmt.","Entry Type"::"Negative Adjmt."]) THEN
          TESTFIELD("Phys. Inventory",FALSE);

        IF CurrFieldNo <> 0 THEN
          WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION("Entry Type"));

        CASE "Entry Type" OF
          "Entry Type"::Purchase:
            IF UserMgt.GetRespCenter(1,'') <> '' THEN
              "Location Code" := UserMgt.GetLocation(1,'',UserMgt.GetPurchasesFilter);
          "Entry Type"::Sale:
            BEGIN
              IF UserMgt.GetRespCenter(0,'') <> '' THEN
                "Location Code" := UserMgt.GetLocation(0,'',UserMgt.GetSalesFilter);
              CheckItemAvailable(FIELDNO("Entry Type"));
            END;
          "Entry Type"::Consumption,"Entry Type"::Output:
            VALIDATE("Order Type","Order Type"::Production);
          "Entry Type"::"Assembly Consumption","Entry Type"::"Assembly Output":
            VALIDATE("Order Type","Order Type"::Assembly);
        END;

        IF xRec."Location Code" = '' THEN
          IF Location.GET("Location Code") THEN
            IF  Location."Directed Put-away and Pick" THEN
              "Location Code" := '';

        IF "Item No." <> '' THEN
          VALIDATE("Location Code");

        VALIDATE("Item No.");
        IF "Entry Type" <> "Entry Type"::Transfer THEN BEGIN
          "New Location Code" := '';
          "New Bin Code" := '';
        END;

        IF "Entry Type" <> "Entry Type"::Output THEN
          Type := Type::" ";

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if not ("Entry Type" in ["Entry Type"::"Positive Adjmt.","Entry Type"::"Negative Adjmt."]) then
          TestField("Phys. Inventory",false);

        if CurrFieldNo <> 0 then
          WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FieldCaption("Entry Type"));

        case "Entry Type" of
          "Entry Type"::Purchase:
            if UserMgt.GetRespCenter(1,'') <> '' then
              "Location Code" := UserMgt.GetLocation(1,'',UserMgt.GetPurchasesFilter);
          "Entry Type"::Sale:
            begin
              if UserMgt.GetRespCenter(0,'') <> '' then
                "Location Code" := UserMgt.GetLocation(0,'',UserMgt.GetSalesFilter);
              CheckItemAvailable(FieldNo("Entry Type"));
            end;
          "Entry Type"::Consumption,"Entry Type"::Output:
            Validate("Order Type","Order Type"::Production);
          "Entry Type"::"Assembly Consumption","Entry Type"::"Assembly Output":
            Validate("Order Type","Order Type"::Assembly);
        end;

        if xRec."Location Code" = '' then
          if Location.Get("Location Code") then
            if  Location."Directed Put-away and Pick" then
              "Location Code" := '';

        if "Item No." <> '' then
          Validate("Location Code");

        Validate("Item No.");
        if "Entry Type" <> "Entry Type"::Transfer then begin
          "New Location Code" := '';
          "New Bin Code" := '';
        end;

        if "Entry Type" <> "Entry Type"::Output then
        #38..40
        */
        //end;


        //Unsupported feature: CodeModification on ""Location Code"(Field 9).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Entry Type" <= "Entry Type"::Transfer THEN
          TESTFIELD("Item No.");

        // Location code in allowed only for inventoriable items
        IF "Location Code" <> '' THEN
          Item.TESTFIELD(Type,Item.Type::Inventory);

        IF ("Value Entry Type" = "Value Entry Type"::"Direct Cost") AND
           ("Item Charge No." = '') AND
           ("No." = '')
        THEN BEGIN
          GetUnitAmount(FIELDNO("Location Code"));
          "Unit Cost" := UnitCost;
          VALIDATE("Unit Amount");
          CheckItemAvailable(FIELDNO("Location Code"));
        END;

        IF "Entry Type" IN ["Entry Type"::Consumption,"Entry Type"::Output] THEN
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        IF "Location Code" <> xRec."Location Code" THEN BEGIN
          "Bin Code" := '';
          IF CurrFieldNo <> 0 THEN
            WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION("Location Code"));
          IF ("Location Code" <> '') AND ("Item No." <> '') THEN BEGIN
            GetLocation("Location Code");
            IF Location."Bin Mandatory" AND NOT Location."Directed Put-away and Pick" THEN
              WMSManagement.GetDefaultBin("Item No.","Variant Code","Location Code","Bin Code");
          END;
          IF "Entry Type" = "Entry Type"::Transfer THEN BEGIN
            "New Location Code" := "Location Code";
            "New Bin Code" := "Bin Code";
          END;
        END;

        VALIDATE("Unit of Measure Code");

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Entry Type" <= "Entry Type"::Transfer then
          TestField("Item No.");

        // Location code in allowed only for inventoriable items
        if "Location Code" <> '' then
          Item.TestField(Type,Item.Type::Inventory);

        if ("Value Entry Type" = "Value Entry Type"::"Direct Cost") and
           ("Item Charge No." = '') and
           ("No." = '')
        then begin
          GetUnitAmount(FieldNo("Location Code"));
          "Unit Cost" := UnitCost;
          Validate("Unit Amount");
          CheckItemAvailable(FieldNo("Location Code"));
        end;

        if "Entry Type" in ["Entry Type"::Consumption,"Entry Type"::Output] then
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        if "Location Code" <> xRec."Location Code" then begin
          "Bin Code" := '';
          if CurrFieldNo <> 0 then
            WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FieldCaption("Location Code"));
          if ("Location Code" <> '') and ("Item No." <> '') then begin
            GetLocation("Location Code");
            if Location."Bin Mandatory" and not Location."Directed Put-away and Pick" then
              WMSManagement.GetDefaultBin("Item No.","Variant Code","Location Code","Bin Code");
          end;
          if "Entry Type" = "Entry Type"::Transfer then begin
            "New Location Code" := "Location Code";
            "New Bin Code" := "Bin Code";
          end;
        end;

        Validate("Unit of Measure Code");

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;


        //Unsupported feature: CodeModification on "Quantity(Field 13).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF ("Entry Type" <= "Entry Type"::Transfer) AND (Quantity <> 0) THEN
          TESTFIELD("Item No.");

        IF NOT PhysInvtEntered THEN
          TESTFIELD("Phys. Inventory",FALSE);

        CallWhseCheck :=
          ("Entry Type" = "Entry Type"::"Assembly Consumption") OR
          ("Entry Type" = "Entry Type"::Consumption) OR
          ("Entry Type" = "Entry Type"::Output) AND
          LastOutputOperation(Rec);
        IF CallWhseCheck THEN
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        IF CurrFieldNo <> 0 THEN
          WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION(Quantity));

        "Quantity (Base)" := UOMMgt.CalcBaseQty(Quantity,"Qty. per Unit of Measure");
        IF ("Entry Type" = "Entry Type"::Output) AND
           ("Value Entry Type" <> "Value Entry Type"::Revaluation)
        THEN
          "Invoiced Quantity" := 0
        ELSE
          "Invoiced Quantity" := Quantity;
        "Invoiced Qty. (Base)" := UOMMgt.CalcBaseQty("Invoiced Quantity","Qty. per Unit of Measure");

        OnValidateQuantityOnBeforeGetUnitAmount(Rec,xRec,CurrFieldNo);

        GetUnitAmount(FIELDNO(Quantity));
        UpdateAmount;

        CheckItemAvailable(FIELDNO(Quantity));

        IF "Entry Type" = "Entry Type"::Transfer THEN BEGIN
          "Qty. (Calculated)" := 0;
          "Qty. (Phys. Inventory)" := 0;
          "Last Item Ledger Entry No." := 0;
        END;

        CALCFIELDS("Reserved Qty. (Base)");
        IF ABS("Quantity (Base)") < ABS("Reserved Qty. (Base)") THEN
          ERROR(Text001,FIELDCAPTION("Reserved Qty. (Base)"));

        IF Item."Item Tracking Code" <> '' THEN
          ReserveItemJnlLine.VerifyQuantity(Rec,xRec);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        */
        //end;


        //Unsupported feature: CodeModification on ""Unit Amount"(Field 16).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        UpdateAmount;
        IF "Item No." <> '' THEN
          IF "Value Entry Type" = "Value Entry Type"::Revaluation THEN
            "Unit Cost" := "Unit Amount"
          ELSE
            CASE "Entry Type" OF
              "Entry Type"::Purchase,
              "Entry Type"::"Positive Adjmt.",
              "Entry Type"::"Assembly Output":
                BEGIN
                  IF "Entry Type" = "Entry Type"::"Positive Adjmt." THEN BEGIN
                    GetItem;
                    IF (CurrFieldNo = FIELDNO("Unit Amount")) AND
                       (Item."Costing Method" = Item."Costing Method"::Standard)
                    THEN
                      ERROR(
                        Text002,
                        FIELDCAPTION("Unit Amount"),Item.FIELDCAPTION("Costing Method"),Item."Costing Method");
                  END;

                  ReadGLSetup;
                  IF "Entry Type" = "Entry Type"::Purchase THEN
                    "Unit Cost" := "Unit Amount";
                  IF "Entry Type" = "Entry Type"::"Positive Adjmt." THEN
                    "Unit Cost" :=
                      ROUND(
                        "Unit Amount" * (1 + "Indirect Cost %" / 100),GLSetup."Unit-Amount Rounding Precision") +
                      "Overhead Rate" * "Qty. per Unit of Measure";
                  IF ("Value Entry Type" = "Value Entry Type"::"Direct Cost") AND
                     ("Item Charge No." = '')
                  THEN
                    VALIDATE("Unit Cost");
                END;
              "Entry Type"::"Negative Adjmt.",
              "Entry Type"::Consumption,
              "Entry Type"::"Assembly Consumption":
                BEGIN
                  GetItem;
                  IF (CurrFieldNo = FIELDNO("Unit Amount")) AND
                     (Item."Costing Method" = Item."Costing Method"::Standard)
                  THEN
                    ERROR(
                      Text002,
                      FIELDCAPTION("Unit Amount"),Item.FIELDCAPTION("Costing Method"),Item."Costing Method");
                  "Unit Cost" := "Unit Amount";
                  IF ("Value Entry Type" = "Value Entry Type"::"Direct Cost") AND
                     ("Item Charge No." = '')
                  THEN
                    VALIDATE("Unit Cost");
                END;
            END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        UpdateAmount;
        if "Item No." <> '' then
          if "Value Entry Type" = "Value Entry Type"::Revaluation then
            "Unit Cost" := "Unit Amount"
          else
            case "Entry Type" of
        #7..9
                begin
                  if "Entry Type" = "Entry Type"::"Positive Adjmt." then begin
                    GetItem;
                    if (CurrFieldNo = FieldNo("Unit Amount")) and
                       (Item."Costing Method" = Item."Costing Method"::Standard)
                    then
                      Error(
                        Text002,
                        FieldCaption("Unit Amount"),Item.FieldCaption("Costing Method"),Item."Costing Method");
                  end;

                  ReadGLSetup;
                  if "Entry Type" = "Entry Type"::Purchase then
                    "Unit Cost" := "Unit Amount";
                  if "Entry Type" = "Entry Type"::"Positive Adjmt." then
                    "Unit Cost" :=
                      Round(
                        "Unit Amount" * (1 + "Indirect Cost %" / 100),GLSetup."Unit-Amount Rounding Precision") +
                      "Overhead Rate" * "Qty. per Unit of Measure";
                  if ("Value Entry Type" = "Value Entry Type"::"Direct Cost") and
                     ("Item Charge No." = '')
                  then
                    Validate("Unit Cost");
                end;
        #34..36
                begin
                  GetItem;
                  if (CurrFieldNo = FieldNo("Unit Amount")) and
                     (Item."Costing Method" = Item."Costing Method"::Standard)
                  then
                    Error(
                      Text002,
                      FieldCaption("Unit Amount"),Item.FieldCaption("Costing Method"),Item."Costing Method");
                  "Unit Cost" := "Unit Amount";
                  if ("Value Entry Type" = "Value Entry Type"::"Direct Cost") and
                     ("Item Charge No." = '')
                  then
                    Validate("Unit Cost");
                end;
            end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Unit Cost"(Field 17).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Item No.");
        RetrieveCosts;
        IF "Entry Type" IN ["Entry Type"::Purchase,"Entry Type"::"Positive Adjmt.","Entry Type"::Consumption] THEN
          IF Item."Costing Method" = Item."Costing Method"::Standard THEN BEGIN
            IF CurrFieldNo = FIELDNO("Unit Cost") THEN
              ERROR(
                Text002,
                FIELDCAPTION("Unit Cost"),Item.FIELDCAPTION("Costing Method"),Item."Costing Method");
            "Unit Cost" := ROUND(UnitCost * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
          END;

        IF ("Item Charge No." = '') AND
           ("Value Entry Type" = "Value Entry Type"::"Direct Cost") AND
           (CurrFieldNo = FIELDNO("Unit Cost"))
        THEN BEGIN
          CASE "Entry Type" OF
            "Entry Type"::Purchase:
              "Unit Amount" := "Unit Cost";
            "Entry Type"::"Positive Adjmt.",
            "Entry Type"::"Assembly Output":
              BEGIN
                ReadGLSetup;
                "Unit Amount" :=
                  ROUND(
                    ("Unit Cost" - "Overhead Rate" * "Qty. per Unit of Measure") / (1 + "Indirect Cost %" / 100),
                    GLSetup."Unit-Amount Rounding Precision")
              END;
            "Entry Type"::"Negative Adjmt.",
            "Entry Type"::Consumption,
            "Entry Type"::"Assembly Consumption":
              BEGIN
                IF Item."Costing Method" = Item."Costing Method"::Standard THEN
                  ERROR(
                    Text002,
                    FIELDCAPTION("Unit Cost"),Item.FIELDCAPTION("Costing Method"),Item."Costing Method");
                "Unit Amount" := "Unit Cost";
              END;
          END;
          UpdateAmount;
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Item No.");
        RetrieveCosts;
        if "Entry Type" in ["Entry Type"::Purchase,"Entry Type"::"Positive Adjmt.","Entry Type"::Consumption] then
          if Item."Costing Method" = Item."Costing Method"::Standard then begin
            if CurrFieldNo = FieldNo("Unit Cost") then
              Error(
                Text002,
                FieldCaption("Unit Cost"),Item.FieldCaption("Costing Method"),Item."Costing Method");
            "Unit Cost" := Round(UnitCost * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
          end;

        if ("Item Charge No." = '') and
           ("Value Entry Type" = "Value Entry Type"::"Direct Cost") and
           (CurrFieldNo = FieldNo("Unit Cost"))
        then begin
          case "Entry Type" of
        #17..20
              begin
                ReadGLSetup;
                "Unit Amount" :=
                  Round(
                    ("Unit Cost" - "Overhead Rate" * "Qty. per Unit of Measure") / (1 + "Indirect Cost %" / 100),
                    GLSetup."Unit-Amount Rounding Precision")
              end;
        #28..30
              begin
                if Item."Costing Method" = Item."Costing Method"::Standard then
                  Error(
                    Text002,
                    FieldCaption("Unit Cost"),Item.FieldCaption("Costing Method"),Item."Costing Method");
                "Unit Amount" := "Unit Cost";
              end;
          end;
          UpdateAmount;
        end;
        */
        //end;


        //Unsupported feature: CodeModification on "Amount(Field 18).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD(Quantity);
        "Unit Amount" := Amount / Quantity;
        VALIDATE("Unit Amount");
        ReadGLSetup;
        "Unit Amount" := ROUND("Unit Amount",GLSetup."Unit-Amount Rounding Precision");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField(Quantity);
        "Unit Amount" := Amount / Quantity;
        Validate("Unit Amount");
        ReadGLSetup;
        "Unit Amount" := Round("Unit Amount",GLSetup."Unit-Amount Rounding Precision");
        */
        //end;


        //Unsupported feature: CodeModification on ""Salespers./Purch. Code"(Field 23).OnValidate". Please convert manually.

        //trigger /Purch();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF ("Order Type" <> "Order Type"::Production) OR ("Order No." = '') THEN
          CreateDim(
            DATABASE::"Salesperson/Purchaser","Salespers./Purch. Code",
            DATABASE::Item,"Item No.",
            DATABASE::"Work Center","Work Center No.");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if ("Order Type" <> "Order Type"::Production) or ("Order No." = '') then
        #2..5
        */
        //end;


        //Unsupported feature: CodeModification on ""Applies-to Entry"(Field 29).OnLookup". Please convert manually.

        //trigger OnLookup();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        SelectItemEntry(FIELDNO("Applies-to Entry"));
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        SelectItemEntry(FieldNo("Applies-to Entry"));
        */
        //end;


        //Unsupported feature: CodeModification on ""Applies-to Entry"(Field 29).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Applies-to Entry" <> 0 THEN BEGIN
          ItemLedgEntry.GET("Applies-to Entry");

          IF "Value Entry Type" = "Value Entry Type"::Revaluation THEN BEGIN
            IF "Inventory Value Per" <> "Inventory Value Per"::" " THEN
              ERROR(Text006,FIELDCAPTION("Applies-to Entry"));

            IF "Inventory Value Per" = "Inventory Value Per"::" " THEN
              IF NOT RevaluationPerEntryAllowed("Item No.") THEN
                ERROR(RevaluationPerEntryNotAllowedErr);

            InitRevalJnlLine(ItemLedgEntry);
            ItemLedgEntry.TESTFIELD(Positive,TRUE);
          END ELSE BEGIN
            TESTFIELD(Quantity);
            IF Signed(Quantity) * ItemLedgEntry.Quantity > 0 THEN BEGIN
              IF Quantity > 0 THEN
                FIELDERROR(Quantity,Text030);
              IF Quantity < 0 THEN
                FIELDERROR(Quantity,Text029);
            END;
            IF ItemLedgEntry.TrackingExists THEN
              ERROR(Text033,FIELDCAPTION("Applies-to Entry"),ItemTrackingLines.CAPTION);

            IF NOT ItemLedgEntry.Open THEN
              MESSAGE(Text032,"Applies-to Entry");

            IF "Entry Type" = "Entry Type"::Output THEN BEGIN
              ItemLedgEntry.TESTFIELD("Order Type","Order Type"::Production);
              ItemLedgEntry.TESTFIELD("Order No.","Order No.");
              ItemLedgEntry.TESTFIELD("Order Line No.","Order Line No.");
              ItemLedgEntry.TESTFIELD("Entry Type","Entry Type");
            END;
          END;

          "Location Code" := ItemLedgEntry."Location Code";
          "Variant Code" := ItemLedgEntry."Variant Code";
        END ELSE
          IF "Value Entry Type" = "Value Entry Type"::Revaluation THEN BEGIN
            VALIDATE("Unit Amount",0);
            VALIDATE(Quantity,0);
            "Inventory Value (Calculated)" := 0;
            "Inventory Value (Revalued)" := 0;
            "Location Code" := '';
            "Variant Code" := '';
            "Bin Code" := '';
          END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Applies-to Entry" <> 0 then begin
          ItemLedgEntry.Get("Applies-to Entry");

          if "Value Entry Type" = "Value Entry Type"::Revaluation then begin
            if "Inventory Value Per" <> "Inventory Value Per"::" " then
              Error(Text006,FieldCaption("Applies-to Entry"));

            if "Inventory Value Per" = "Inventory Value Per"::" " then
              if not RevaluationPerEntryAllowed("Item No.") then
                Error(RevaluationPerEntryNotAllowedErr);

            InitRevalJnlLine(ItemLedgEntry);
            ItemLedgEntry.TestField(Positive,true);
          end else begin
            TestField(Quantity);
            if Signed(Quantity) * ItemLedgEntry.Quantity > 0 then begin
              if Quantity > 0 then
                FieldError(Quantity,Text030);
              if Quantity < 0 then
                FieldError(Quantity,Text029);
            end;

            if ItemLedgEntry.TrackingExists then
              Error(Text033,FieldCaption("Applies-to Entry"),ItemTrackingLines.Caption);

            if not ItemLedgEntry.Open then
              Message(Text032,"Applies-to Entry");

            if "Entry Type" = "Entry Type"::Output then begin
              ItemLedgEntry.TestField("Order Type","Order Type"::Production);
              ItemLedgEntry.TestField("Order No.","Order No.");
              ItemLedgEntry.TestField("Order Line No.","Order Line No.");
              ItemLedgEntry.TestField("Entry Type","Entry Type");
            end;
          end;
        #35..37
        end else
          if "Value Entry Type" = "Value Entry Type"::Revaluation then begin
            Validate("Unit Amount",0);
            Validate(Quantity,0);
        #42..46
          end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Indirect Cost %"(Field 37).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Item No.");
        TESTFIELD("Value Entry Type","Value Entry Type"::"Direct Cost");
        TESTFIELD("Item Charge No.",'');
        IF "Entry Type" IN ["Entry Type"::Sale,"Entry Type"::"Negative Adjmt."] THEN
          ERROR(
            Text002,
            FIELDCAPTION("Indirect Cost %"),FIELDCAPTION("Entry Type"),"Entry Type");

        GetItem;
        IF Item."Costing Method" = Item."Costing Method"::Standard THEN
          ERROR(
            Text002,
            FIELDCAPTION("Indirect Cost %"),Item.FIELDCAPTION("Costing Method"),Item."Costing Method");

        IF "Entry Type" <> "Entry Type"::Purchase THEN
          "Unit Cost" :=
            ROUND(
              "Unit Amount" * (1 + "Indirect Cost %" / 100) +
              "Overhead Rate" * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Item No.");
        TestField("Value Entry Type","Value Entry Type"::"Direct Cost");
        TestField("Item Charge No.",'');
        if "Entry Type" in ["Entry Type"::Sale,"Entry Type"::"Negative Adjmt."] then
          Error(
            Text002,
            FieldCaption("Indirect Cost %"),FieldCaption("Entry Type"),"Entry Type");

        GetItem;
        if Item."Costing Method" = Item."Costing Method"::Standard then
          Error(
            Text002,
            FieldCaption("Indirect Cost %"),Item.FieldCaption("Costing Method"),Item."Costing Method");

        if "Entry Type" <> "Entry Type"::Purchase then
          "Unit Cost" :=
            Round(
              "Unit Amount" * (1 + "Indirect Cost %" / 100) +
              "Overhead Rate" * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
        */
        //end;


        //Unsupported feature: CodeModification on ""New Location Code"(Field 50).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Entry Type","Entry Type"::Transfer);
        IF "New Location Code" <> xRec."New Location Code" THEN BEGIN
          "New Bin Code" := '';
          IF ("New Location Code" <> '') AND ("Item No." <> '') THEN BEGIN
            GetLocation("New Location Code");
            IF Location."Bin Mandatory" AND NOT Location."Directed Put-away and Pick" THEN
              WMSManagement.GetDefaultBin("Item No.","Variant Code","New Location Code","New Bin Code")
          END;
        END;

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Entry Type","Entry Type"::Transfer);
        if "New Location Code" <> xRec."New Location Code" then begin
          "New Bin Code" := '';
          if ("New Location Code" <> '') and ("Item No." <> '') then begin
            GetLocation("New Location Code");
            if Location."Bin Mandatory" and not Location."Directed Put-away and Pick" then
              WMSManagement.GetDefaultBin("Item No.","Variant Code","New Location Code","New Bin Code")
          end;
        end;

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;


        //Unsupported feature: CodeModification on ""New Shortcut Dimension 1 Code"(Field 51).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Entry Type","Entry Type"::Transfer);
        ValidateNewShortcutDimCode(1,"New Shortcut Dimension 1 Code");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Entry Type","Entry Type"::Transfer);
        ValidateNewShortcutDimCode(1,"New Shortcut Dimension 1 Code");
        */
        //end;


        //Unsupported feature: CodeModification on ""New Shortcut Dimension 2 Code"(Field 52).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Entry Type","Entry Type"::Transfer);
        ValidateNewShortcutDimCode(2,"New Shortcut Dimension 2 Code");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Entry Type","Entry Type"::Transfer);
        ValidateNewShortcutDimCode(2,"New Shortcut Dimension 2 Code");
        */
        //end;


        //Unsupported feature: CodeModification on ""Qty. (Calculated)"(Field 53).OnValidate". Please convert manually.

        //trigger  (Calculated)"(Field 53)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        VALIDATE("Qty. (Phys. Inventory)");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        Validate("Qty. (Phys. Inventory)");
        */
        //end;


        //Unsupported feature: CodeModification on ""Qty. (Phys. Inventory)"(Field 54).OnValidate". Please convert manually.

        //trigger  (Phys();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Phys. Inventory",TRUE);

        IF CurrFieldNo <> 0 THEN
          WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION("Qty. (Phys. Inventory)"));

        PhysInvtEntered := TRUE;
        Quantity := 0;
        IF "Qty. (Phys. Inventory)" >= "Qty. (Calculated)" THEN BEGIN
          VALIDATE("Entry Type","Entry Type"::"Positive Adjmt.");
          VALIDATE(Quantity,"Qty. (Phys. Inventory)" - "Qty. (Calculated)");
        END ELSE BEGIN
          VALIDATE("Entry Type","Entry Type"::"Negative Adjmt.");
          VALIDATE(Quantity,"Qty. (Calculated)" - "Qty. (Phys. Inventory)");
        END;
        PhysInvtEntered := FALSE;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Phys. Inventory",true);

        if CurrFieldNo <> 0 then
          WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FieldCaption("Qty. (Phys. Inventory)"));

        PhysInvtEntered := true;
        Quantity := 0;
        if "Qty. (Phys. Inventory)" >= "Qty. (Calculated)" then begin
          Validate("Entry Type","Entry Type"::"Positive Adjmt.");
          Validate(Quantity,"Qty. (Phys. Inventory)" - "Qty. (Calculated)");
        end else begin
          Validate("Entry Type","Entry Type"::"Negative Adjmt.");
          Validate(Quantity,"Qty. (Calculated)" - "Qty. (Phys. Inventory)");
        end;
        PhysInvtEntered := false;
        */
        //end;


        //Unsupported feature: CodeModification on ""Order Type"(Field 90).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Order Type" = xRec."Order Type" THEN
          EXIT;
        VALIDATE("Order No.",'');
        "Order Line No." := 0;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Order Type" = xRec."Order Type" then
          exit;
        Validate("Order No.",'');
        "Order Line No." := 0;
        */
        //end;


        //Unsupported feature: CodeModification on ""Order No."(Field 91).OnValidate". Please convert manually.

        //trigger "(Field 91)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        CASE "Order Type" OF
          "Order Type"::Production,"Order Type"::Assembly:
            BEGIN
              IF "Order No." = '' THEN BEGIN
                CASE "Order Type" OF
                  "Order Type"::Production:
                    CreateProdDim;
                  "Order Type"::Assembly:
                    CreateAssemblyDim;
                END;
                EXIT;
              END;

              CASE "Order Type" OF
                "Order Type"::Production:
                  BEGIN
                    GetMfgSetup;
                    IF MfgSetup."Doc. No. Is Prod. Order No." THEN
                      "Document No." := "Order No.";
                    ProdOrder.GET(ProdOrder.Status::Released,"Order No.");
                    ProdOrder.TESTFIELD(Blocked,FALSE);
                    Description := ProdOrder.Description;
                    OnValidateOrderNoOrderTypeProduction(Rec,ProdOrder);
                  END;
                "Order Type"::Assembly:
                  BEGIN
                    AssemblyHeader.GET(AssemblyHeader."Document Type"::Order,"Order No.");
                    Description := AssemblyHeader.Description;
                  END;
              END;

              "Gen. Bus. Posting Group" := ProdOrder."Gen. Bus. Posting Group";
              CASE TRUE OF
                "Entry Type" = "Entry Type"::Output:
                  BEGIN
                    "Inventory Posting Group" := ProdOrder."Inventory Posting Group";
                    "Gen. Prod. Posting Group" := ProdOrder."Gen. Prod. Posting Group";
                  END;
                "Entry Type" = "Entry Type"::"Assembly Output":
                  BEGIN
                    "Inventory Posting Group" := AssemblyHeader."Inventory Posting Group";
                    "Gen. Prod. Posting Group" := AssemblyHeader."Gen. Prod. Posting Group";
                  END;
                "Entry Type" = "Entry Type"::Consumption:
                  BEGIN
                    ProdOrderLine.SetFilterByReleasedOrderNo("Order No.");
                    IF ProdOrderLine.COUNT = 1 THEN BEGIN
                      ProdOrderLine.FINDFIRST;
                      VALIDATE("Order Line No.",ProdOrderLine."Line No.");
                    END;
                  END;
              END;

              IF ("Order No." <> xRec."Order No.") OR ("Order Type" <> xRec."Order Type") THEN
                CASE "Order Type" OF
                  "Order Type"::Production:
                    CreateProdDim;
                  "Order Type"::Assembly:
                    CreateAssemblyDim;
                END;
            END;
          "Order Type"::Transfer,"Order Type"::Service,"Order Type"::" ":
            ERROR(Text002,FIELDCAPTION("Order No."),FIELDCAPTION("Order Type"),"Order Type");
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        case "Order Type" of
          "Order Type"::Production,"Order Type"::Assembly:
            begin
              if "Order No." = '' then begin
                case "Order Type" of
        #6..9
                end;
                exit;
              end;

              case "Order Type" of
                "Order Type"::Production:
                  begin
                    GetMfgSetup;
                    if MfgSetup."Doc. No. Is Prod. Order No." then
                      "Document No." := "Order No.";
                    ProdOrder.Get(ProdOrder.Status::Released,"Order No.");
                    ProdOrder.TestField(Blocked,false);
                    Description := ProdOrder.Description;
                    OnValidateOrderNoOrderTypeProduction(Rec,ProdOrder);
                  end;
                "Order Type"::Assembly:
                  begin
                    AssemblyHeader.Get(AssemblyHeader."Document Type"::Order,"Order No.");
                    Description := AssemblyHeader.Description;
                  end;
              end;

              "Gen. Bus. Posting Group" := ProdOrder."Gen. Bus. Posting Group";
              case true of
                "Entry Type" = "Entry Type"::Output:
                  begin
                    "Inventory Posting Group" := ProdOrder."Inventory Posting Group";
                    "Gen. Prod. Posting Group" := ProdOrder."Gen. Prod. Posting Group";
                  end;
                "Entry Type" = "Entry Type"::"Assembly Output":
                  begin
                    "Inventory Posting Group" := AssemblyHeader."Inventory Posting Group";
                    "Gen. Prod. Posting Group" := AssemblyHeader."Gen. Prod. Posting Group";
                  end;
                "Entry Type" = "Entry Type"::Consumption:
                  begin
                    ProdOrderLine.SetFilterByReleasedOrderNo("Order No.");
                    if ProdOrderLine.Count = 1 then begin
                      ProdOrderLine.FindFirst;
                      Validate("Order Line No.",ProdOrderLine."Line No.");
                    end;
                  end;
              end;

              if ("Order No." <> xRec."Order No.") or ("Order Type" <> xRec."Order Type") then
                case "Order Type" of
        #56..59
                end;
            end;
          "Order Type"::Transfer,"Order Type"::Service,"Order Type"::" ":
            Error(Text002,FieldCaption("Order No."),FieldCaption("Order Type"),"Order Type");
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Order Line No."(Field 92).OnValidate". Please convert manually.

        //trigger "(Field 92)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Order No.");
        CASE "Order Type" OF
          "Order Type"::Production,"Order Type"::Assembly:
            BEGIN
              IF "Order Type" = "Order Type"::Production THEN BEGIN
                ProdOrderLine.SetFilterByReleasedOrderNo("Order No.");
                ProdOrderLine.SETRANGE("Line No.","Order Line No.");
                IF ProdOrderLine.FINDFIRST THEN BEGIN
                  "Source Type" := "Source Type"::Item;
                  "Source No." := ProdOrderLine."Item No.";
                  "Order Line No." := ProdOrderLine."Line No.";
                  "Routing No." := ProdOrderLine."Routing No.";
                  "Routing Reference No." := ProdOrderLine."Routing Reference No.";
                  IF "Entry Type" = "Entry Type"::Output THEN BEGIN
                    "Location Code" := ProdOrderLine."Location Code";
                    "Bin Code" := ProdOrderLine."Bin Code";
                  END;
                END;
              END;

              IF "Order Line No." <> xRec."Order Line No." THEN
                CASE "Order Type" OF
                  "Order Type"::Production:
                    CreateProdDim;
                  "Order Type"::Assembly:
                    CreateAssemblyDim;
                END;
            END;
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Order No.");
        case "Order Type" of
          "Order Type"::Production,"Order Type"::Assembly:
            begin
              if "Order Type" = "Order Type"::Production then begin
                ProdOrderLine.SetFilterByReleasedOrderNo("Order No.");
                ProdOrderLine.SetRange("Line No.","Order Line No.");
                if ProdOrderLine.FindFirst then begin
        #9..13
                  if "Entry Type" = "Entry Type"::Output then begin
                    "Location Code" := ProdOrderLine."Location Code";
                    "Bin Code" := ProdOrderLine."Bin Code";
                  end;
                end;
              end;

              if "Order Line No." <> xRec."Order Line No." then
                case "Order Type" of
        #23..26
                end;
            end;
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Variant Code"(Field 5402).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Entry Type" IN ["Entry Type"::Consumption,"Entry Type"::Output] THEN
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        IF "Variant Code" <> xRec."Variant Code" THEN BEGIN
          "Bin Code" := '';
          IF CurrFieldNo <> 0 THEN
            WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION("Variant Code"));
          IF ("Location Code" <> '') AND ("Item No." <> '') THEN BEGIN
            GetLocation("Location Code");
            IF Location."Bin Mandatory" AND NOT Location."Directed Put-away and Pick" THEN
              WMSManagement.GetDefaultBin("Item No.","Variant Code","Location Code","Bin Code")
          END;
          IF ("Entry Type" = "Entry Type"::Transfer) AND ("Location Code" = "New Location Code") THEN
            "New Bin Code" := "Bin Code";
        END;
        IF ("Value Entry Type" = "Value Entry Type"::"Direct Cost") AND
           ("Item Charge No." = '')
        THEN BEGIN
          GetUnitAmount(FIELDNO("Variant Code"));
          "Unit Cost" := UnitCost;
          VALIDATE("Unit Amount");
          VALIDATE("Unit of Measure Code");
          ReserveItemJnlLine.VerifyChange(Rec,xRec);
        END;

        IF "Variant Code" <> '' THEN BEGIN
          ItemVariant.GET("Item No.","Variant Code");
          Description := ItemVariant.Description;
        END ELSE BEGIN
          GetItem;
          Description := Item.Description;
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Entry Type" in ["Entry Type"::Consumption,"Entry Type"::Output] then
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        if "Variant Code" <> xRec."Variant Code" then begin
          "Bin Code" := '';
          if CurrFieldNo <> 0 then
            WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FieldCaption("Variant Code"));
          if ("Location Code" <> '') and ("Item No." <> '') then begin
            GetLocation("Location Code");
            if Location."Bin Mandatory" and not Location."Directed Put-away and Pick" then
              WMSManagement.GetDefaultBin("Item No.","Variant Code","Location Code","Bin Code")
          end;
          if ("Entry Type" = "Entry Type"::Transfer) and ("Location Code" = "New Location Code") then
            "New Bin Code" := "Bin Code";
        end;
        if ("Value Entry Type" = "Value Entry Type"::"Direct Cost") and
           ("Item Charge No." = '')
        then begin
          GetUnitAmount(FieldNo("Variant Code"));
          "Unit Cost" := UnitCost;
          Validate("Unit Amount");
          Validate("Unit of Measure Code");
          ReserveItemJnlLine.VerifyChange(Rec,xRec);
        end;

        if "Variant Code" <> '' then begin
          ItemVariant.Get("Item No.","Variant Code");
          Description := ItemVariant."Part No";
        end else begin
          GetItem;
          Description := Item.Description;
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Bin Code"(Field 5403).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Bin Code" <> xRec."Bin Code" THEN BEGIN
          TESTFIELD("Location Code");
          IF "Bin Code" <> '' THEN BEGIN
            GetBin("Location Code","Bin Code");
            GetLocation("Location Code");
            Location.TESTFIELD("Bin Mandatory");
            IF CurrFieldNo <> 0 THEN
              WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION("Bin Code"));
            TESTFIELD("Location Code",Bin."Location Code");
            WhseIntegrationMgt.CheckBinTypeCode(DATABASE::"Item Journal Line",
              FIELDCAPTION("Bin Code"),
              "Location Code",
              "Bin Code",
              "Entry Type");
          END;
          IF ("Entry Type" = "Entry Type"::Transfer) AND ("Location Code" = "New Location Code") THEN
            "New Bin Code" := "Bin Code";

          IF ("Entry Type" = "Entry Type"::Consumption) AND
             ("Bin Code" <> '') AND ("Prod. Order Comp. Line No." <> 0)
          THEN BEGIN
            TESTFIELD("Order Type","Order Type"::Production);
            TESTFIELD("Order No.");
            ProdOrderComp.GET(ProdOrderComp.Status::Released,"Order No.","Order Line No.","Prod. Order Comp. Line No.");
            IF (ProdOrderComp."Bin Code" <> '') AND (ProdOrderComp."Bin Code" <> "Bin Code") THEN
              IF NOT CONFIRM(
                   Text021,
                   FALSE,
                   "Bin Code",
                   ProdOrderComp."Bin Code",
                   "Order No.")
              THEN
                ERROR(UpdateInterruptedErr);
          END;
        END;

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Bin Code" <> xRec."Bin Code" then begin
          TestField("Location Code");
          if "Bin Code" <> '' then begin
            GetBin("Location Code","Bin Code");
            GetLocation("Location Code");
            Location.TestField("Bin Mandatory");
            if CurrFieldNo <> 0 then
              WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FieldCaption("Bin Code"));
            TestField("Location Code",Bin."Location Code");
            WhseIntegrationMgt.CheckBinTypeCode(DATABASE::"Item Journal Line",
              FieldCaption("Bin Code"),
        #12..14
          end;
          if ("Entry Type" = "Entry Type"::Transfer) and ("Location Code" = "New Location Code") then
            "New Bin Code" := "Bin Code";

          if ("Entry Type" = "Entry Type"::Consumption) and
             ("Bin Code" <> '') and ("Prod. Order Comp. Line No." <> 0)
          then begin
            TestField("Order Type","Order Type"::Production);
            TestField("Order No.");
            ProdOrderComp.Get(ProdOrderComp.Status::Released,"Order No.","Order Line No.","Prod. Order Comp. Line No.");
            if (ProdOrderComp."Bin Code" <> '') and (ProdOrderComp."Bin Code" <> "Bin Code") then
              if not Confirm(
                   Text021,
                   false,
        #29..31
              then
                Error(UpdateInterruptedErr);
          end;
        end;

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;


        //Unsupported feature: CodeModification on ""New Bin Code"(Field 5406).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Entry Type","Entry Type"::Transfer);
        IF "New Bin Code" <> xRec."New Bin Code" THEN BEGIN
          TESTFIELD("New Location Code");
          IF "New Bin Code" <> '' THEN BEGIN
            GetBin("New Location Code","New Bin Code");
            GetLocation("New Location Code");
            Location.TESTFIELD("Bin Mandatory");
            IF CurrFieldNo <> 0 THEN
              WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION("New Bin Code"));
            TESTFIELD("New Location Code",Bin."Location Code");
            WhseIntegrationMgt.CheckBinTypeCode(DATABASE::"Item Journal Line",
              FIELDCAPTION("New Bin Code"),
              "New Location Code",
              "New Bin Code",
              "Entry Type");
          END;
        END;

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Entry Type","Entry Type"::Transfer);
        if "New Bin Code" <> xRec."New Bin Code" then begin
          TestField("New Location Code");
          if "New Bin Code" <> '' then begin
            GetBin("New Location Code","New Bin Code");
            GetLocation("New Location Code");
            Location.TestField("Bin Mandatory");
            if CurrFieldNo <> 0 then
              WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FieldCaption("New Bin Code"));
            TestField("New Location Code",Bin."Location Code");
            WhseIntegrationMgt.CheckBinTypeCode(DATABASE::"Item Journal Line",
              FieldCaption("New Bin Code"),
        #13..15
          end;
        end;

        ReserveItemJnlLine.VerifyChange(Rec,xRec);
        */
        //end;


        //Unsupported feature: CodeModification on ""Unit of Measure Code"(Field 5407).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        OnBeforeValidateUnitOfMeasureCode(Rec,IsHandled);
        IF IsHandled THEN
          EXIT;

        GetItem;
        "Qty. per Unit of Measure" := UOMMgt.GetQtyPerUnitOfMeasure(Item,"Unit of Measure Code");

        IF "Entry Type" IN ["Entry Type"::Consumption,"Entry Type"::Output] THEN
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        IF CurrFieldNo <> 0 THEN
          WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FIELDCAPTION("Unit of Measure Code"));

        GetUnitAmount(FIELDNO("Unit of Measure Code"));
        IF "Value Entry Type" = "Value Entry Type"::Revaluation THEN
          TESTFIELD("Qty. per Unit of Measure",1);

        ReadGLSetup;
        IsHandled := FALSE;
        OnValidateUnitOfMeasureCodeOnBeforeCalcUnitCost(Rec,UnitCost,IsHandled);
        IF NOT IsHandled THEN
          "Unit Cost" := ROUND(UnitCost * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");

        IF "Entry Type" = "Entry Type"::Consumption THEN BEGIN
          "Indirect Cost %" := ROUND(Item."Indirect Cost %" * "Qty. per Unit of Measure",1);
          "Overhead Rate" :=
            ROUND(Item."Overhead Rate" * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
          "Unit Amount" := ROUND(UnitCost * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
        END;

        IF "No." <> '' THEN
          VALIDATE("Cap. Unit of Measure Code");

        VALIDATE("Unit Amount");

        IF "Entry Type" = "Entry Type"::Output THEN BEGIN
          VALIDATE("Output Quantity");
          VALIDATE("Scrap Quantity");
        END ELSE
          VALIDATE(Quantity);

        CheckItemAvailable(FIELDNO("Unit of Measure Code"));
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        OnBeforeValidateUnitOfMeasureCode(Rec,IsHandled);
        if IsHandled then
          exit;
        #4..7
        if "Entry Type" in ["Entry Type"::Consumption,"Entry Type"::Output] then
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        if CurrFieldNo <> 0 then
          WMSManagement.CheckItemJnlLineFieldChange(Rec,xRec,FieldCaption("Unit of Measure Code"));

        GetUnitAmount(FieldNo("Unit of Measure Code"));
        if "Value Entry Type" = "Value Entry Type"::Revaluation then
          TestField("Qty. per Unit of Measure",1);

        ReadGLSetup;
        IsHandled := false;
        OnValidateUnitOfMeasureCodeOnBeforeCalcUnitCost(Rec,UnitCost,IsHandled);
        if not IsHandled then
          "Unit Cost" := Round(UnitCost * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");

        if "Entry Type" = "Entry Type"::Consumption then begin
          "Indirect Cost %" := Round(Item."Indirect Cost %" * "Qty. per Unit of Measure",1);
          "Overhead Rate" :=
            Round(Item."Overhead Rate" * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
          "Unit Amount" := Round(UnitCost * "Qty. per Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
        end;

        if "No." <> '' then
          Validate("Cap. Unit of Measure Code");

        Validate("Unit Amount");

        if "Entry Type" = "Entry Type"::Output then begin
          Validate("Output Quantity");
          Validate("Scrap Quantity");
        end else
          Validate(Quantity);

        CheckItemAvailable(FieldNo("Unit of Measure Code"));
        */
        //end;


        //Unsupported feature: CodeModification on ""Quantity (Base)"(Field 5413).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Qty. per Unit of Measure",1);
        VALIDATE(Quantity,"Quantity (Base)");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Qty. per Unit of Measure",1);
        Validate(Quantity,"Quantity (Base)");
        */
        //end;


        //Unsupported feature: CodeModification on ""Inventory Value (Calculated)"(Field 5802).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        ReadGLSetup;
        "Unit Cost (Calculated)" :=
          ROUND("Inventory Value (Calculated)" / Quantity,GLSetup."Unit-Amount Rounding Precision");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        ReadGLSetup;
        "Unit Cost (Calculated)" :=
          Round("Inventory Value (Calculated)" / Quantity,GLSetup."Unit-Amount Rounding Precision");
        */
        //end;


        //Unsupported feature: CodeModification on ""Inventory Value (Revalued)"(Field 5803).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Value Entry Type","Value Entry Type"::Revaluation);
        VALIDATE(Amount,"Inventory Value (Revalued)" - "Inventory Value (Calculated)");
        ReadGLSetup;
        IF ("Unit Cost (Revalued)" <> xRec."Unit Cost (Revalued)") OR
           ("Inventory Value (Revalued)" <> xRec."Inventory Value (Revalued)")
        THEN BEGIN
          IF CurrFieldNo <> FIELDNO("Unit Cost (Revalued)") THEN
            "Unit Cost (Revalued)" :=
              ROUND("Inventory Value (Revalued)" / Quantity,GLSetup."Unit-Amount Rounding Precision");

          IF CurrFieldNo <> 0 THEN
            ClearSingleAndRolledUpCosts;
        END
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Value Entry Type","Value Entry Type"::Revaluation);
        Validate(Amount,"Inventory Value (Revalued)" - "Inventory Value (Calculated)");
        ReadGLSetup;
        if ("Unit Cost (Revalued)" <> xRec."Unit Cost (Revalued)") or
           ("Inventory Value (Revalued)" <> xRec."Inventory Value (Revalued)")
        then begin
          if CurrFieldNo <> FieldNo("Unit Cost (Revalued)") then
            "Unit Cost (Revalued)" :=
              Round("Inventory Value (Revalued)" / Quantity,GLSetup."Unit-Amount Rounding Precision");

          if CurrFieldNo <> 0 then
            ClearSingleAndRolledUpCosts;
        end
        */
        //end;


        //Unsupported feature: CodeModification on ""Applies-from Entry"(Field 5807).OnLookup". Please convert manually.

        //trigger OnLookup();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        SelectItemEntry(FIELDNO("Applies-from Entry"));
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        SelectItemEntry(FieldNo("Applies-from Entry"));
        */
        //end;


        //Unsupported feature: CodeModification on ""Applies-from Entry"(Field 5807).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Applies-from Entry" <> 0 THEN BEGIN
          TESTFIELD(Quantity);
          IF Signed(Quantity) < 0 THEN BEGIN
            IF Quantity > 0 THEN
              FIELDERROR(Quantity,Text030);
            IF Quantity < 0 THEN
              FIELDERROR(Quantity,Text029);
          END;
          ItemLedgEntry.GET("Applies-from Entry");
          ItemLedgEntry.TESTFIELD(Positive,FALSE);
          IF ItemLedgEntry.TrackingExists THEN
            ERROR(Text033,FIELDCAPTION("Applies-from Entry"),ItemTrackingLines.CAPTION);
          "Unit Cost" := CalcUnitCost(ItemLedgEntry);
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Applies-from Entry" <> 0 then begin
          TestField(Quantity);
          if Signed(Quantity) < 0 then begin
            if Quantity > 0 then
              FieldError(Quantity,Text030);
            if Quantity < 0 then
              FieldError(Quantity,Text029);
          end;
          ItemLedgEntry.Get("Applies-from Entry");
          ItemLedgEntry.TestField(Positive,false);
          if ItemLedgEntry.TrackingExists then
            Error(Text033,FieldCaption("Applies-from Entry"),ItemTrackingLines.Caption);
          "Unit Cost" := CalcUnitCost(ItemLedgEntry);
        end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Unit Cost (Calculated)"(Field 5809).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Value Entry Type","Value Entry Type"::Revaluation);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Value Entry Type","Value Entry Type"::Revaluation);
        */
        //end;


        //Unsupported feature: CodeModification on ""Unit Cost (Revalued)"(Field 5810).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        ReadGLSetup;
        TESTFIELD("Value Entry Type","Value Entry Type"::Revaluation);
        IF "Unit Cost (Revalued)" <> xRec."Unit Cost (Revalued)" THEN
          VALIDATE(
            "Inventory Value (Revalued)",
            ROUND(
              "Unit Cost (Revalued)" * Quantity,GLSetup."Amount Rounding Precision"));
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        ReadGLSetup;
        TestField("Value Entry Type","Value Entry Type"::Revaluation);
        if "Unit Cost (Revalued)" <> xRec."Unit Cost (Revalued)" then
          Validate(
            "Inventory Value (Revalued)",
            Round(
              "Unit Cost (Revalued)" * Quantity,GLSetup."Amount Rounding Precision"));
        */
        //end;


        //Unsupported feature: CodeModification on ""Update Standard Cost"(Field 5812).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Inventory Value Per");
        GetItem;
        Item.TESTFIELD("Costing Method",Item."Costing Method"::Standard);
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Inventory Value Per");
        GetItem;
        Item.TestField("Costing Method",Item."Costing Method"::Standard);
        */
        //end;


        //Unsupported feature: CodeModification on "Type(Field 5830).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF Type = Type::Resource THEN
          TESTFIELD("Entry Type","Entry Type"::"Assembly Output")
        ELSE
          TESTFIELD("Entry Type","Entry Type"::Output);
        VALIDATE("No.",'');
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if Type = Type::Resource then
          TestField("Entry Type","Entry Type"::"Assembly Output")
        else
          TestField("Entry Type","Entry Type"::Output);
        Validate("No.",'');
        */
        //end;


        //Unsupported feature: CodeModification on ""No."(Field 5831).OnValidate". Please convert manually.

        //trigger "(Field 5831)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF Type = Type::Resource THEN
          TESTFIELD("Entry Type","Entry Type"::"Assembly Output")
        ELSE
          TESTFIELD("Entry Type","Entry Type"::Output);
        IF "No." = '' THEN BEGIN
          "Work Center No." := '';
          "Work Center Group Code" := '';
          VALIDATE("Item No.");
          IF Type IN [Type::"Work Center",Type::"Machine Center"] THEN
            CreateDimWithProdOrderLine
          ELSE
            CreateDim(
              DATABASE::"Work Center","Work Center No.",
              DATABASE::Item,"Item No.",
              DATABASE::"Salesperson/Purchaser","Salespers./Purch. Code");
          EXIT;
        END;

        CASE Type OF
          Type::"Work Center":
            BEGIN
              WorkCenter.GET("No.");
              WorkCenter.TESTFIELD(Blocked,FALSE);
              CopyFromWorkCenter(WorkCenter);
            END;
          Type::"Machine Center":
            BEGIN
              MachineCenter.GET("No.");
              MachineCenter.TESTFIELD(Blocked,FALSE);
              WorkCenter.GET(MachineCenter."Work Center No.");
              WorkCenter.TESTFIELD(Blocked,FALSE);
              CopyFromMachineCenter(MachineCenter);
            END;
          Type::Resource:
            BEGIN
              Resource.GET("No.");
              Resource.CheckResourcePrivacyBlocked(FALSE);
              Resource.TESTFIELD(Blocked,FALSE);
            END;
        END;

        IF Type IN [Type::"Work Center",Type::"Machine Center"] THEN BEGIN
          "Work Center No." := WorkCenter."No.";
          "Work Center Group Code" := WorkCenter."Work Center Group Code";
          VALIDATE("Cap. Unit of Measure Code",WorkCenter."Unit of Measure Code");
        END;

        IF "Work Center No." <> '' THEN
          CreateDimWithProdOrderLine;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if Type = Type::Resource then
          TestField("Entry Type","Entry Type"::"Assembly Output")
        else
          TestField("Entry Type","Entry Type"::Output);
        if "No." = '' then begin
          "Work Center No." := '';
          "Work Center Group Code" := '';
          Validate("Item No.");
          if Type in [Type::"Work Center",Type::"Machine Center"] then
            CreateDimWithProdOrderLine
          else
        #12..15
          exit;
        end;

        case Type of
          Type::"Work Center":
            begin
              WorkCenter.Get("No.");
              WorkCenter.TestField(Blocked,false);
              CopyFromWorkCenter(WorkCenter);
            end;
          Type::"Machine Center":
            begin
              MachineCenter.Get("No.");
              MachineCenter.TestField(Blocked,false);
              WorkCenter.Get(MachineCenter."Work Center No.");
              WorkCenter.TestField(Blocked,false);
              CopyFromMachineCenter(MachineCenter);
            end;
          Type::Resource:
            begin
              Resource.Get("No.");
              Resource.CheckResourcePrivacyBlocked(false);
              Resource.TestField(Blocked,false);
            end;
        end;

        if Type in [Type::"Work Center",Type::"Machine Center"] then begin
          "Work Center No." := WorkCenter."No.";
          "Work Center Group Code" := WorkCenter."Work Center Group Code";
          Validate("Cap. Unit of Measure Code",WorkCenter."Unit of Measure Code");
        end;

        if "Work Center No." <> '' then
          CreateDimWithProdOrderLine;
        */
        //end;


        //Unsupported feature: CodeModification on ""Operation No."(Field 5838).OnValidate". Please convert manually.

        //trigger "(Field 5838)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Entry Type","Entry Type"::Output);
        IF "Operation No." = '' THEN
          EXIT;

        TESTFIELD("Order Type","Order Type"::Production);
        TESTFIELD("Order No.");
        TESTFIELD("Item No.");

        ConfirmOutputOnFinishedOperation;
        GetProdOrderRtngLine(ProdOrderRtngLine);

        CASE ProdOrderRtngLine.Type OF
          ProdOrderRtngLine.Type::"Work Center":
            Type := Type::"Work Center";
          ProdOrderRtngLine.Type::"Machine Center":
            Type := Type::"Machine Center";
        END;
        VALIDATE("No.",ProdOrderRtngLine."No.");
        Description := ProdOrderRtngLine.Description;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Entry Type","Entry Type"::Output);
        if "Operation No." = '' then
          exit;

        TestField("Order Type","Order Type"::Production);
        TestField("Order No.");
        TestField("Item No.");
        #8..11
        case ProdOrderRtngLine.Type of
        #13..16
        end;
        Validate("No.",ProdOrderRtngLine."No.");
        Description := ProdOrderRtngLine.Description;
        "Operation Description" := ProdOrderRtngLine."Operation Description";
        "Planed Setup Time" := ProdOrderRtngLine."Setup Time";
        if ("Journal Batch Name"='PONE-RE')  or ("Journal Batch Name"='PTWO-RE')  or ("Journal Batch Name"='PTHREE-RE')  or
           ("Journal Batch Name"='PFOUR-RE') or ("Journal Batch Name"='MPR-RE') or ("Journal Batch Name"='SHF-RE') then
        begin
          "Planed Run Time" :=0;
          //EFFUPG Start
          {
          ProdOrderLine.SETRANGE(ProdOrderLine."Prod. Order No.","Order No.");
          ProdOrderLine.SETRANGE(ProdOrderLine."Line No.","Order Line No.");
          IF ProdOrderLine.FINDFIRST THEN
          BEGIN
            IF ProdOrderLine.Quantity=1 THEN     // According to Code The First Person Who Worked On the Compound Will
            BEGIN                               // Come As Rewokr User Id . and it is also approved by Sita rani Mam
              CLE.SETCURRENTKEY(CLE."Order No.",CLE."Order Line No.",
                                CLE."Routing No.",CLE."Routing Reference No.",
                                CLE."Operation No.",CLE."Last Output Line");
              CLE.SETRANGE(CLE."Order No.","Order No.");
              CLE.SETRANGE(CLE."Order Line No.","Order Line No.");
              CLE.SETRANGE(CLE."Operation No.","Operation No.");
              IF CLE.FINDFIRST THEN
                 "Reworked User Id":=CLE."No.";
            END;
          END;
          }
          //EFFUPG End Need to develop the code with technical team
        end else
        "Planed Run Time" := ProdOrderRtngLine."Run Time";
        "Planed Wait Time" := ProdOrderRtngLine."Wait Time";
        "Planed Move Time" := ProdOrderRtngLine."Move Time";
        */
        //end;


        //Unsupported feature: CodeModification on ""Setup Time"(Field 5841).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF SubcontractingWorkCenterUsed AND ("Setup Time" <> 0) THEN
          ERROR(SubcontractedErr,FIELDCAPTION("Setup Time"),"Line No.");
        "Setup Time (Base)" := CalcBaseTime("Setup Time");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if SubcontractingWorkCenterUsed and ("Setup Time" <> 0) then
          Error(SubcontractedErr,FieldCaption("Setup Time"),"Line No.");
        "Setup Time (Base)" := CalcBaseTime("Setup Time");
        */
        //end;


        //Unsupported feature: CodeModification on ""Run Time"(Field 5842).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF SubcontractingWorkCenterUsed AND ("Run Time" <> 0) THEN
          ERROR(SubcontractedErr,FIELDCAPTION("Run Time"),"Line No.");

        "Run Time (Base)" := CalcBaseTime("Run Time");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if SubcontractingWorkCenterUsed and ("Run Time" <> 0) then
          Error(SubcontractedErr,FieldCaption("Run Time"),"Line No.");

        "Run Time (Base)" := CalcBaseTime("Run Time");
        */
        //end;


        //Unsupported feature: CodeModification on ""Output Quantity"(Field 5846).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Entry Type","Entry Type"::Output);
        IF SubcontractingWorkCenterUsed AND ("Output Quantity" <> 0) THEN
          ERROR(SubcontractedErr,FIELDCAPTION("Output Quantity"),"Line No.");

        ConfirmOutputOnFinishedOperation;

        IF LastOutputOperation(Rec) THEN
          WhseValidateSourceLine.ItemLineVerifyChange(Rec,xRec);

        "Output Quantity (Base)" := UOMMgt.CalcBaseQty("Output Quantity","Qty. per Unit of Measure");

        VALIDATE(Quantity,"Output Quantity");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Entry Type","Entry Type"::Output);
        if SubcontractingWorkCenterUsed and ("Output Quantity" <> 0) then
          Error(SubcontractedErr,FieldCaption("Output Quantity"),"Line No.");
        #4..6
        if LastOutputOperation(Rec) then
        #8..11
        Validate(Quantity,"Output Quantity");
        */
        //end;


        //Unsupported feature: CodeModification on ""Scrap Quantity"(Field 5847).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Entry Type","Entry Type"::Output);
        "Scrap Quantity (Base)" := UOMMgt.CalcBaseQty("Scrap Quantity","Qty. per Unit of Measure");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Entry Type","Entry Type"::Output);
        "Scrap Quantity (Base)" := UOMMgt.CalcBaseQty("Scrap Quantity","Qty. per Unit of Measure");
        */
        //end;


        //Unsupported feature: CodeModification on ""Concurrent Capacity"(Field 5849).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Entry Type","Entry Type"::Output);
        IF "Concurrent Capacity" = 0 THEN
          EXIT;

        TESTFIELD("Starting Time");
        TESTFIELD("Ending Time");
        TotalTime := CalendarMgt.CalcTimeDelta("Ending Time","Starting Time");
        IF "Ending Time" < "Starting Time" THEN
          TotalTime := TotalTime + 86400000;
        TESTFIELD("Work Center No.");
        WorkCenter.GET("Work Center No.");
        VALIDATE("Setup Time",0);
        VALIDATE(
          "Run Time",
          ROUND(
            TotalTime / CalendarMgt.TimeFactor("Cap. Unit of Measure Code") *
            "Concurrent Capacity",WorkCenter."Calendar Rounding Precision"));
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Entry Type","Entry Type"::Output);
        if "Concurrent Capacity" = 0 then
          exit;

        TestField("Starting Time");
        TestField("Ending Time");
        TotalTime := CalendarMgt.CalcTimeDelta("Ending Time","Starting Time");
        if "Ending Time" < "Starting Time" then
          TotalTime := TotalTime + 86400000;
        TestField("Work Center No.");
        WorkCenter.Get("Work Center No.");
        Validate("Setup Time",0);
        Validate(
          "Run Time",
          Round(
            TotalTime / CalendarMgt.TimeFactor("Cap. Unit of Measure Code") *
            "Concurrent Capacity",WorkCenter."Calendar Rounding Precision"));
        */
        //end;


        //Unsupported feature: CodeModification on ""Setup Time (Base)"(Field 5851).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Qty. per Cap. Unit of Measure",1);
        VALIDATE("Setup Time","Setup Time (Base)");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Qty. per Cap. Unit of Measure",1);
        Validate("Setup Time","Setup Time (Base)");
        */
        //end;


        //Unsupported feature: CodeModification on ""Run Time (Base)"(Field 5852).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Qty. per Cap. Unit of Measure",1);
        VALIDATE("Run Time","Run Time (Base)");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Qty. per Cap. Unit of Measure",1);
        Validate("Run Time","Run Time (Base)");
        */
        //end;


        //Unsupported feature: CodeModification on ""Stop Time (Base)"(Field 5853).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Qty. per Cap. Unit of Measure",1);
        VALIDATE("Stop Time","Stop Time (Base)");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Qty. per Cap. Unit of Measure",1);
        Validate("Stop Time","Stop Time (Base)");
        */
        //end;


        //Unsupported feature: CodeModification on ""Output Quantity (Base)"(Field 5856).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Qty. per Unit of Measure",1);
        VALIDATE("Output Quantity","Output Quantity (Base)");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Qty. per Unit of Measure",1);
        Validate("Output Quantity","Output Quantity (Base)");
        */
        //end;


        //Unsupported feature: CodeModification on ""Scrap Quantity (Base)"(Field 5857).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        TESTFIELD("Qty. per Unit of Measure",1);
        VALIDATE("Scrap Quantity","Scrap Quantity (Base)");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        TestField("Qty. per Unit of Measure",1);
        Validate("Scrap Quantity","Scrap Quantity (Base)");
        */
        //end;


        //Unsupported feature: CodeModification on ""Cap. Unit of Measure Code"(Field 5858).OnValidate". Please convert manually.

        //trigger  Unit of Measure Code"(Field 5858)();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF Type <> Type::Resource THEN BEGIN
          "Qty. per Cap. Unit of Measure" :=
            ROUND(
              CalendarMgt.QtyperTimeUnitofMeasure(
                "Work Center No.","Cap. Unit of Measure Code"),
              UOMMgt.QtyRndPrecision);

          VALIDATE("Setup Time");
          VALIDATE("Run Time");
          VALIDATE("Stop Time");
        END;

        IF "Order No." <> '' THEN
          CASE "Order Type" OF
            "Order Type"::Production:
              BEGIN
                GetProdOrderRtngLine(ProdOrderRtngLine);
                "Unit Cost" := ProdOrderRtngLine."Unit Cost per";

                CostCalcMgt.RoutingCostPerUnit(
                  Type,"No.","Unit Amount","Indirect Cost %","Overhead Rate","Unit Cost","Unit Cost Calculation");
              END;
            "Order Type"::Assembly:
              CostCalcMgt.ResourceCostPerUnit("No.","Unit Amount","Indirect Cost %","Overhead Rate","Unit Cost");
          END;

        ReadGLSetup;
        "Unit Cost" :=
          ROUND("Unit Cost" * "Qty. per Cap. Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
        "Unit Amount" :=
          ROUND("Unit Amount" * "Qty. per Cap. Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
        VALIDATE("Unit Amount");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if Type <> Type::Resource then begin
          "Qty. per Cap. Unit of Measure" :=
            Round(
        #4..7
          Validate("Setup Time");
          Validate("Run Time");
          Validate("Stop Time");
        end;

        if "Order No." <> '' then
          case "Order Type" of
            "Order Type"::Production:
              begin
        #17..21
              end;
            "Order Type"::Assembly:
              CostCalcMgt.ResourceCostPerUnit("No.","Unit Amount","Indirect Cost %","Overhead Rate","Unit Cost");
          end;
        #26..28
          Round("Unit Cost" * "Qty. per Cap. Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
        "Unit Amount" :=
          Round("Unit Amount" * "Qty. per Cap. Unit of Measure",GLSetup."Unit-Amount Rounding Precision");
        Validate("Unit Amount");
        */
        //end;


        //Unsupported feature: CodeModification on ""Starting Time"(Field 5873).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Ending Time" < "Starting Time" THEN
          "Ending Time" := "Starting Time";

        VALIDATE("Concurrent Capacity");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Ending Time" < "Starting Time" then
          "Ending Time" := "Starting Time";

        Validate("Concurrent Capacity");
        */
        //end;


        //Unsupported feature: CodeModification on ""Ending Time"(Field 5874).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        VALIDATE("Concurrent Capacity");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        Validate("Concurrent Capacity");
        */
        //end;


        //Unsupported feature: CodeModification on ""Prod. Order Comp. Line No."(Field 5884).OnValidate". Please convert manually.

        //trigger  Order Comp();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF "Prod. Order Comp. Line No." <> xRec."Prod. Order Comp. Line No." THEN BEGIN
          IF ("Order Type" = "Order Type"::Production) AND ("Prod. Order Comp. Line No." <> 0) THEN BEGIN
            ProdOrderComponent.GET(
              ProdOrderComponent.Status::Released,"Order No.","Order Line No.","Prod. Order Comp. Line No.");
            IF "Item No." <> ProdOrderComponent."Item No." THEN
              VALIDATE("Item No.",ProdOrderComponent."Item No.");
          END;

          CreateProdDim;
        END;
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if "Prod. Order Comp. Line No." <> xRec."Prod. Order Comp. Line No." then begin
          if ("Order Type" = "Order Type"::Production) and ("Prod. Order Comp. Line No." <> 0) then begin
            ProdOrderComponent.Get(
              ProdOrderComponent.Status::Released,"Order No.","Order Line No.","Prod. Order Comp. Line No.");
            if "Item No." <> ProdOrderComponent."Item No." then
              Validate("Item No.",ProdOrderComponent."Item No.");
          end;

          CreateProdDim;
        end;
        */
        //end;


        //Unsupported feature: CodeInsertion on ""Stop Code"(Field 5895)". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //begin
        /*
                                                           TestField(Type,Type::"Machine Center");
                                                           if  "Stop Code"='LEAVES' then
                                                             "Cap. Unit of Measure Code":='Days'
                                                           else if  "Stop Code"='PERMISSIONS' then
                                                           begin
                                                              "Cap. Unit of Measure Code":='Hours';
                                                              TestField("Stop Time",1);
                                                           end;
        */
        //end;


        //Unsupported feature: CodeModification on ""Scrap Code"(Field 5896).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IsHandled := FALSE;
        OnBeforeValidateScrapCode(Rec,IsHandled);
        IF IsHandled THEN
          EXIT;

        TESTFIELD(Type,Type::"Machine Center");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        IsHandled := false;
        OnBeforeValidateScrapCode(Rec,IsHandled);
        if IsHandled then
          exit;

        TestField(Type,Type::"Machine Center");
        */
        //end;


        //Unsupported feature: CodeModification on ""Overhead Rate"(Field 99000755).OnValidate". Please convert manually.

        //trigger OnValidate();
        //Parameters and return type have not been exported.
        //>>>> ORIGINAL CODE:
        //begin
        /*
        IF ("Value Entry Type" <> "Value Entry Type"::"Direct Cost") OR
           ("Item Charge No." <> '')
        THEN BEGIN
          "Overhead Rate" := 0;
          VALIDATE("Indirect Cost %",0);
        END ELSE
          VALIDATE("Indirect Cost %");
        */
        //end;
        //>>>> MODIFIED CODE:
        //begin
        /*
        if ("Value Entry Type" <> "Value Entry Type"::"Direct Cost") or
           ("Item Charge No." <> '')
        then begin
          "Overhead Rate" := 0;
          Validate("Indirect Cost %",0);
        end else
          Validate("Indirect Cost %");
        */
        //end;
        field(13702; "BED %"; Decimal)
        {
            CaptionML = ENU = 'BED %',
                        ENN = 'BED %';
            DataClassification = ToBeClassified;
        }
        field(13703; "BED Amount"; Decimal)
        {
            CaptionML = ENU = 'BED Amount',
                        ENN = 'BED Amount';
            DataClassification = ToBeClassified;
        }
        field(13704; "Other Duties %"; Decimal)
        {
            CaptionML = ENU = 'Other Duties %',
                        ENN = 'Other Duties %';
            DataClassification = ToBeClassified;
        }
        field(13705; "Other Duties Amount"; Decimal)
        {
            CaptionML = ENU = 'Other Duties Amount',
                        ENN = 'Other Duties Amount';
            DataClassification = ToBeClassified;
        }
        field(13710; "Reverse Excise"; Boolean)
        {
            CaptionML = ENU = 'Reverse Excise',
                        ENN = 'Reverse Excise';
            DataClassification = ToBeClassified;
        }
        field(13711; "RG23PartI Entry No."; Integer)
        {
            CaptionML = ENU = 'RG23PartI Entry No.',
                        ENN = 'RG23PartI Entry No.';
            DataClassification = ToBeClassified;
        }
        field(13712; "RG23PartII Entry No."; Integer)
        {
            CaptionML = ENU = 'RG23PartII Entry No.',
                        ENN = 'RG23PartII Entry No.';
            DataClassification = ToBeClassified;
        }
        field(13713; Return; Boolean)
        {
            CaptionML = ENU = 'Return',
                        ENN = 'Return';
            DataClassification = ToBeClassified;
        }
        field(13715; "RG Correction Type"; Option)
        {
            CaptionML = ENU = 'RG Correction Type',
                        ENN = 'RG Correction Type';
            DataClassification = ToBeClassified;
            OptionCaptionML = ENU = 'Purchase,Sale',
                              ENN = 'Purchase,Sale';
            OptionMembers = Purchase,Sale;
        }
        field(13716; "RG Line No."; Integer)
        {
            CaptionML = ENU = 'RG Line No.',
                        ENN = 'RG Line No.';
            DataClassification = ToBeClassified;
        }
        field(13717; "Excise Record"; Option)
        {
            CaptionML = ENU = 'Excise Record',
                        ENN = 'Excise Record';
            DataClassification = ToBeClassified;
            OptionCaptionML = ENU = ',RG23A,RG23C',
                              ENN = ',RG23A,RG23C';
            OptionMembers = ,RG23A,RG23C;
        }
        field(16500; "PO/SO Line No."; Integer)
        {
            CaptionML = ENU = 'PO/SO Line No.',
                        ENN = 'PO/SO Line No.';
            DataClassification = ToBeClassified;
        }
        field(16501; "Applies-to Entry (RG 23 D)"; Integer)
        {
            CaptionML = ENU = 'Applies-to Entry (RG 23 D)',
                        ENN = 'Applies-to Entry (RG 23 D)';
            DataClassification = ToBeClassified;
            TableRelation = "RG 23 D"."Entry No." WHERE("Location Code" = FIELD("Location Code"),
                                                         "Item No." = FIELD("Item No."),
                                                         Closed = FILTER(false),
                                                         "Transaction Type" = FILTER(Purchase),
                                                         "Transfer From Location Code" = FILTER(''),
                                                         "Transfer To Location Code" = FILTER(''),
                                                         "Item Ledg. Entry No." = FIELD("Applies-to Entry"));

            trigger OnValidate();
            begin
                //CheckRG23D;
            end;
        }
        field(16502; "Excise Entry"; Boolean)
        {
            CaptionML = ENU = 'Excise Entry',
                        ENN = 'Excise Entry';
            DataClassification = ToBeClassified;
        }
        field(16503; "Laboratory Test"; Boolean)
        {
            CaptionML = ENU = 'Laboratory Test',
                        ENN = 'Laboratory Test';
            DataClassification = ToBeClassified;

            trigger OnValidate();
            var
                Location: Record Location;
                TestLocation: Boolean;
            begin
                /*IF "Laboratory Test" = TRUE THEN BEGIN
                  TestLocation := TRUE;
                  Location.RESET;
                  Location.SETFILTER(Code,'%1|%2',"Location Code","New Location Code");
                  IF Location.FINDSET THEN BEGIN
                    REPEAT
                      IF Location."Testing Location" = TRUE THEN
                        TestLocation:= FALSE;
                    UNTIL Location.NEXT = 0;
                  IF TestLocation = TRUE THEN
                    ERROR(Text16501);
                  END;
                END;
                */

            end;
        }
        field(16504; "Other Usage"; Option)
        {
            CaptionML = ENU = 'Other Usage',
                        ENN = 'Other Usage';
            DataClassification = ToBeClassified;
            OptionCaptionML = ENU = ' ,Disposal,Wasted/Destroyed',
                              ENN = ' ,Disposal,Wasted/Destroyed';
            OptionMembers = " ",Disposal,"Wasted/Destroyed";

            trigger OnValidate();
            begin
                /*IF "Other Usage" = "Other Usage"::"Wasted/Destroyed" THEN BEGIN
                  "Nature of Disposal" := '';
                  MODIFY;
                END;*/

            end;
        }
        field(16505; "Nature of Disposal"; Code[10])
        {
            CaptionML = ENU = 'Nature of Disposal',
                        ENN = 'Nature of Disposal';
            DataClassification = ToBeClassified;
            TableRelation = "Type of Disposal".Code WHERE(Type = FILTER("Negative Adjustment"));
        }
        field(16506; "Type of Disposal"; Option)
        {
            CaptionML = ENU = 'Type of Disposal',
                        ENN = 'Type of Disposal';
            DataClassification = ToBeClassified;
            OptionCaptionML = ENU = ' ,Purchase return,Transfer Shipment,Sale,Negative Adjustment',
                              ENN = ' ,Purchase return,Transfer Shipment,Sale,Negative Adjustment';
            OptionMembers = " ","Purchase return","Transfer Shipment",Sale,"Negative Adjustment";
        }
        field(16507; "Captive Consumption"; Boolean)
        {
            CaptionML = ENU = 'Captive Consumption',
                        ENN = 'Captive Consumption';
            DataClassification = ToBeClassified;
        }
        field(16508; "From Transfer Order"; Boolean)
        {
            CaptionML = ENU = 'From Transfer Order',
                        ENN = 'From Transfer Order';
            DataClassification = ToBeClassified;
        }
        field(16509; "Re-Dispatch"; Boolean)
        {
            CaptionML = ENU = 'Re-Dispatch',
                        ENN = 'Re-Dispatch';
            DataClassification = ToBeClassified;
        }
        field(16510; "Assessable Value"; Decimal)
        {
            CaptionML = ENU = 'Assessable Value',
                        ENN = 'Assessable Value';
            DataClassification = ToBeClassified;
        }
        field(16511; "Rcpt. Posting"; Boolean)
        {
            CaptionML = ENU = 'Rcpt. Posting',
                        ENN = 'Rcpt. Posting';
            DataClassification = ToBeClassified;
        }
        field(16512; "Transfer Cost"; Decimal)
        {
            AutoFormatType = 2;
            CaptionML = ENU = 'Transfer Cost',
                        ENN = 'Transfer Cost';
            DataClassification = ToBeClassified;
            Editable = false;
        }
        field(16515; "Subcon Order No."; Code[20])
        {
            CaptionML = ENU = 'Subcon Order No.',
                        ENN = 'Subcon Order No.';
            DataClassification = ToBeClassified;
        }
        field(16600; "GST Availment (Goods)"; Boolean)
        {
            CaptionML = ENU = 'GST Availment (Goods)',
                        ENN = 'GST Availment (Goods)';
            DataClassification = ToBeClassified;
            Editable = false;
        }
        field(16601; "GST Adjustment Entry"; Boolean)
        {
            CaptionML = ENU = 'GST Adjustment Entry',
                        ENN = 'GST Adjustment Entry';
            DataClassification = ToBeClassified;
        }
        field(16602; "Source Template Name"; Code[20])
        {
            CaptionML = ENU = 'Source Template Name',
                        ENN = 'Source Template Name';
            DataClassification = ToBeClassified;
        }
        field(60001; "Operation Description"; Text[50])
        {
            Description = 'B2B';
            Editable = false;
        }
        field(60002; "Planed Setup Time"; Decimal)
        {
            Caption = 'Planed Setup Time';
            DecimalPlaces = 0 : 5;
            Description = 'B2B';
            Editable = false;
            MinValue = 0;
        }
        field(60003; "Planed Run Time"; Decimal)
        {
            Caption = 'Planed Run Time';
            DecimalPlaces = 0 : 5;
            Description = 'B2B';
            Editable = false;
            MinValue = 0;
        }
        field(60004; "Planed Wait Time"; Decimal)
        {
            Caption = 'Planed Wait Time';
            DecimalPlaces = 0 : 5;
            Description = 'B2B';
            Editable = false;
            MinValue = 0;
        }
        field(60005; "Planed Move Time"; Decimal)
        {
            Caption = 'Planed Move Time';
            DecimalPlaces = 0 : 5;
            Description = 'B2B';
            Editable = false;
            MinValue = 0;
        }
        field(60006; "Internal Rework"; Boolean)
        {
            Description = 'B2B';
        }
        field(60007; "QC Rework"; Boolean)
        {
            Description = 'B2B';
        }
        field(60008; "Quantity."; Decimal)
        {
            DecimalPlaces = 0 : 5;
            Description = 'B2B';
        }
        field(60009; "Qty. Received"; Decimal)
        {
            BlankZero = true;
            CalcFormula = Sum("Material Issue Line"."Received Quantity" WHERE("Material Issue Doc No" = FIELD("Document No."),
                                                                               "Line No." = FIELD("Line No.")));
            DecimalPlaces = 0 : 5;
            Editable = false;
            FieldClass = FlowField;
        }
        field(60010; "Transfer Type"; Option)
        {
            Description = 'B2B';
            OptionMembers = " ","Material Issue";
        }
        field(60011; "Shelf No."; Code[50])
        {
            Description = 'B2B';
        }
        field(60012; "Job No.2"; Code[20])
        {
        }
        field(60013; "Job Budget Line No."; Integer)
        {
        }
        field(60014; "Serial No. EFF"; Code[20])
        {
            Description = 'B2B';
            TableRelation = "Prod. Order Comp Resource"."Serial No." WHERE("Prod. Order No." = FIELD("Order No."),
                                                                            "Prod. Order Line No." = FIELD("Order Line No."));
        }
        field(60100; "ITL Doc No."; Code[20])
        {
            Caption = 'Prod. Ord No.';
            Description = 'B2B';
            Editable = false;
        }
        field(60101; "ITL Doc Line No."; Integer)
        {
            Caption = 'Prod. Ord Line No.';
            Description = 'B2B';
            Editable = false;
        }
        field(60103; "ITL Doc Ref Line No."; Integer)
        {
            Caption = 'Prod.Ord Comp. Line No.';
            Description = 'B2B';
            Editable = false;
        }
        field(60104; "Qty. At Location Code"; Decimal)
        {
            Editable = false;
            FieldClass = Normal;
        }
        field(60105; "User ID"; Code[50])
        {
            Description = 'B2B,Rev01';
            Editable = false;
            TableRelation = User."User Name";
            //This property is currently not supported
            //TestTableRelation = false;
        }
        field(60106; Remarks; Text[250])
        {
            Description = 'B2B';
        }
        field(60107; "Output Jr Serial No."; Code[20])
        {
            Description = 'B2B';
            Editable = false;
        }
        field(60108; "Finished Product Sr No"; Code[20])
        {
            Description = 'B2B';
        }
        field(60109; "Issued Date Time"; DateTime)
        {
        }
        field(60110; "Reworked User Id"; Code[15])
        {
            TableRelation = IF (Type = CONST("Machine Center")) "Machine Center"
            ELSE
            IF (Type = CONST("Work Center")) "Work Center";
        }
        field(60116; "Sales Order No"; Code[20])
        {
            Description = 'SH1.0';
            TableRelation = "Sales Header"."No." WHERE("Document Type" = CONST(Order));
        }
        field(60117; "Sales Order Line No"; Integer)
        {
            Description = 'SH1.0';
            TableRelation = "Sales Line"."Line No." WHERE("Document No." = FIELD("Sales Order No"));
        }
        field(60118; "Schedule Line No"; Integer)
        {
            Description = 'SH1.0';
            TableRelation = Schedule2."Line No." WHERE("Document No." = FIELD("Sales Order No"),
                                                        "Document Line No." = FIELD("Sales Order Line No"));
        }
        field(80100; Assigned; Boolean)
        {
        }
        field(90000; Level2; Integer)
        {
            Description = 'AJAY';
        }
        field(90001; "Changed by User2"; Boolean)
        {
            Description = 'AJAY';
        }
        field(33000251; "Quality Ledger Entry No."; Integer)
        {
            Description = 'QC1.2';
            TableRelation = "Quality Ledger Entry";
        }
        field(33000252; "After Inspection"; Boolean)
        {
            Description = 'QC1.2';
        }
        field(33000253; "Inspectin Receipt No."; Code[20])
        {
            Description = 'QC1.2';
            TableRelation = "Inspection Receipt Header"."No.";
        }
        field(33000260; "Purch.Rcpt Line"; Integer)
        {
            Description = 'QC1.2';
        }
        field(33000261; "QC Check"; Boolean)
        {
            Description = 'B2BQCCheck';
        }
    }
    keys
    {

        //Unsupported feature: Deletion on ""Journal Template Name,Journal Batch Name,Line No."(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Entry Type,Item No.,Variant Code,Location Code,Bin Code,Posting Date"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Entry Type,Item No.,Variant Code,New Location Code,New Bin Code,Posting Date"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Item No.,Posting Date"(Key)". Please convert manually.


        //Unsupported feature: Deletion on ""Journal Template Name,Journal Batch Name,Item No.,Location Code,Variant Code"(Key)". Please convert manually.

        key(Key1; "Journal Template Name", "Journal Batch Name", "Line No.")
        {
            MaintainSIFTIndex = false;
        }
        key(Key2; "Entry Type", "Item No.", "Variant Code", "Location Code", "Bin Code", "Posting Date")
        {
            MaintainSIFTIndex = false;
            SumIndexFields = "Quantity (Base)";
        }
        key(Key3; "Entry Type", "Item No.", "Variant Code", "New Location Code", "New Bin Code", "Posting Date")
        {
            MaintainSIFTIndex = false;
            SumIndexFields = "Quantity (Base)";
        }
        key(Key4; "Item No.", "Posting Date")
        {
        }
        key(Key5; "Journal Template Name", "Journal Batch Name", "Item No.", "Location Code", "Variant Code")
        {
        }
        key(Key6; "ITL Doc No.", "ITL Doc Line No.", "ITL Doc Ref Line No.")
        {
            SumIndexFields = Quantity;
        }
        key(Key7; "Document No.", "Line No.")
        {
        }
        key(Key8; "Line No.")
        {
        }
        key(Key9; "Journal Template Name", "Journal Batch Name", "Document No.", "Line No.")
        {
        }
    }


    //Unsupported feature: CodeModification on "OnDelete". Please convert manually.

    //trigger OnDelete();
    //Parameters and return type have not been exported.
    //>>>> ORIGINAL CODE:
    //begin
    /*
    ReserveItemJnlLine.DeleteLine(Rec);

    CALCFIELDS("Reserved Qty. (Base)");
    TESTFIELD("Reserved Qty. (Base)",0);
    */
    //end;
    //>>>> MODIFIED CODE:
    //begin
    /*
    ReserveItemJnlLine.DeleteLine(Rec);

    CalcFields("Reserved Qty. (Base)");
    TestField("Reserved Qty. (Base)",0);
    */
    //end;


    //Unsupported feature: CodeModification on "OnInsert". Please convert manually.

    //trigger OnInsert();
    //Parameters and return type have not been exported.
    //>>>> ORIGINAL CODE:
    //begin
    /*
    LOCKTABLE;
    ItemJnlTemplate.GET("Journal Template Name");
    ItemJnlBatch.GET("Journal Template Name","Journal Batch Name");

    ValidateShortcutDimCode(1,"Shortcut Dimension 1 Code");
    ValidateShortcutDimCode(2,"Shortcut Dimension 2 Code");
    ValidateNewShortcutDimCode(1,"New Shortcut Dimension 1 Code");
    ValidateNewShortcutDimCode(2,"New Shortcut Dimension 2 Code");

    CheckPlanningAssignment;
    */
    //end;
    //>>>> MODIFIED CODE:
    //begin
    /*
    LockTable;
    ItemJnlTemplate.Get("Journal Template Name");
    ItemJnlBatch.Get("Journal Template Name","Journal Batch Name");
    #4..10
    if "Journal Batch Name"='MPR' then
     "Location Code":='MPR'
    else if "Journal Batch Name"='STR' then
     "Location Code":='STR'
    else if "Journal Batch Name"='SHF' then
     "Location Code":='SHF';
    */
    //end;


    //Unsupported feature: CodeModification on "OnModify". Please convert manually.

    //trigger OnModify();
    //Parameters and return type have not been exported.
    //>>>> ORIGINAL CODE:
    //begin
    /*
    OnBeforeVerifyReservedQty(Rec,xRec,0);
    ReserveItemJnlLine.VerifyChange(Rec,xRec);
    CheckPlanningAssignment;
    */
    //end;
    //>>>> MODIFIED CODE:
    //begin
    /*
    OnBeforeVerifyReservedQty(Rec,xRec,0);
    ReserveItemJnlLine.VerifyChange(Rec,xRec);
    //NSS 02 >>Begin
    "User ID" := UserId;
    //NSS 02 <<End
    CheckPlanningAssignment;
    */
    //end;

    //Unsupported feature: InsertAfter on "Documentation". Please convert manually.


    //Unsupported feature: PropertyChange. Please convert manually.


    //Unsupported feature: PropertyChange. Please convert manually.


    //Unsupported feature: PropertyChange. Please convert manually.



    //Unsupported feature: PropertyModification on ""Item No."(Field 3).OnValidate.ProdOrderLine(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Item No." : 5406;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Item No." : "Prod. Order Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Item No."(Field 3).OnValidate.ProdOrderComp(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Item No." : 5407;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Item No." : "Prod. Order Component";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Posting Date"(Field 4).OnValidate.CheckDateConflict(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Posting Date" : 99000815;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Posting Date" : "Reservation-Check Date Confl.";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Applies-to Entry"(Field 29).OnValidate.ItemLedgEntry(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Applies-to Entry" : 32;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Applies-to Entry" : "Item Ledger Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Applies-to Entry"(Field 29).OnValidate.ItemTrackingLines(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Applies-to Entry" : 6510;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Applies-to Entry" : "Item Tracking Lines";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Order No."(Field 91).OnValidate.AssemblyHeader(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Order No." : 900;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Order No." : "Assembly Header";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Order No."(Field 91).OnValidate.ProdOrder(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Order No." : 5405;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Order No." : "Production Order";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Order No."(Field 91).OnValidate.ProdOrderLine(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Order No." : 5406;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Order No." : "Prod. Order Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Order Line No."(Field 92).OnValidate.ProdOrderLine(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Order Line No." : 5406;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Order Line No." : "Prod. Order Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bin Code"(Field 5403).OnValidate.ProdOrderComp(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bin Code" : 5407;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bin Code" : "Prod. Order Component";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Bin Code"(Field 5403).OnValidate.WhseIntegrationMgt(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Bin Code" : 7317;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Bin Code" : "Whse. Integration Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""New Bin Code"(Field 5406).OnValidate.WhseIntegrationMgt(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"New Bin Code" : 7317;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"New Bin Code" : "Whse. Integration Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Applies-from Entry"(Field 5807).OnValidate.ItemLedgEntry(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Applies-from Entry" : 32;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Applies-from Entry" : "Item Ledger Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Applies-from Entry"(Field 5807).OnValidate.ItemTrackingLines(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Applies-from Entry" : 6510;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Applies-from Entry" : "Item Tracking Lines";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""No."(Field 5831).OnValidate.Resource(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"No." : 156;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"No." : Resource;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Operation No."(Field 5838).OnValidate.ProdOrderRtngLine(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Operation No." : 5409;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Operation No." : "Prod. Order Routing Line";
    //Variable type has not been exported.

    var
        CapacityLedgerEntry: Record "Capacity Ledger Entry";
        ProdOrderRoutingLine1: Record "Prod. Order Routing Line";


    //Unsupported feature: PropertyModification on ""Cap. Unit of Measure Code"(Field 5858).OnValidate.ProdOrderRtngLine(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Cap. Unit of Measure Code" : 5409;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Cap. Unit of Measure Code" : "Prod. Order Routing Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on ""Prod. Order Comp. Line No."(Field 5884).OnValidate.ProdOrderComponent(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //"Prod. Order Comp. Line No." : 5407;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //"Prod. Order Comp. Line No." : "Prod. Order Component";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "SelectItemEntry(PROCEDURE 3).ItemLedgEntry(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //SelectItemEntry : 32;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //SelectItemEntry : "Item Ledger Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "SelectItemEntry(PROCEDURE 3).ItemJnlLine2(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //SelectItemEntry : 83;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //SelectItemEntry : "Item Journal Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "PickDimension(PROCEDURE 48).ItemJournalTemplate(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //PickDimension : 82;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //PickDimension : "Item Journal Template";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CopyDim(PROCEDURE 46).DimSetEntry(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CopyDim : 480;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CopyDim : "Dimension Set Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CreateProdDim(PROCEDURE 25).ProdOrder(Variable 1008)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CreateProdDim : 5405;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CreateProdDim : "Production Order";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CreateProdDim(PROCEDURE 25).ProdOrderLine(Variable 1009)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CreateProdDim : 5406;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CreateProdDim : "Prod. Order Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CreateProdDim(PROCEDURE 25).ProdOrderComp(Variable 1010)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CreateProdDim : 5407;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CreateProdDim : "Prod. Order Component";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CreateAssemblyDim(PROCEDURE 42).AssemblyHeader(Variable 1008)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CreateAssemblyDim : 900;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CreateAssemblyDim : "Assembly Header";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CreateAssemblyDim(PROCEDURE 42).AssemblyLine(Variable 1009)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CreateAssemblyDim : 901;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CreateAssemblyDim : "Assembly Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CreateDimWithProdOrderLine(PROCEDURE 54).ProdOrderLine(Variable 1005)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CreateDimWithProdOrderLine : 5406;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CreateDimWithProdOrderLine : "Prod. Order Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "InitRevalJnlLine(PROCEDURE 5800).ItemApplnEntry(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //InitRevalJnlLine : 339;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //InitRevalJnlLine : "Item Application Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "InitRevalJnlLine(PROCEDURE 5800).ValueEntry(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //InitRevalJnlLine : 5802;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //InitRevalJnlLine : "Value Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "RetrieveCosts(PROCEDURE 5803).SKU(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //RetrieveCosts : 5700;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //RetrieveCosts : "Stockkeeping Unit";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "RetrieveCosts(PROCEDURE 5803).InventorySetup(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //RetrieveCosts : 313;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //RetrieveCosts : "Inventory Setup";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CalcUnitCost(PROCEDURE 5804).ValueEntry(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CalcUnitCost : 5802;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CalcUnitCost : "Value Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "RowID1(PROCEDURE 44).ItemTrackingMgt(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //RowID1 : 6500;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //RowID1 : "Item Tracking Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ItemPosting(PROCEDURE 33).ProdOrderRoutingLine(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ItemPosting : 5409;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ItemPosting : "Prod. Order Routing Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "LastOutputOperation(PROCEDURE 35).ProdOrderRtngLine(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //LastOutputOperation : 5409;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //LastOutputOperation : "Prod. Order Routing Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "LastOutputOperation(PROCEDURE 35).ItemJnlPostLine(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //LastOutputOperation : 22;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //LastOutputOperation : "Item Jnl.-Post Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "LookupItemNo(PROCEDURE 37).ItemList(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //LookupItemNo : 31;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //LookupItemNo : "Item List";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "LookupProdOrderLine(PROCEDURE 32).ProdOrderLine(Variable 1004)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //LookupProdOrderLine : 5406;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //LookupProdOrderLine : "Prod. Order Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "LookupProdOrderLine(PROCEDURE 32).ProdOrderLineList(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //LookupProdOrderLine : 5406;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //LookupProdOrderLine : "Prod. Order Line List";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "LookupProdOrderComp(PROCEDURE 11).ProdOrderComp(Variable 1004)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //LookupProdOrderComp : 5407;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //LookupProdOrderComp : "Prod. Order Component";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "LookupProdOrderComp(PROCEDURE 11).ProdOrderCompLineList(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //LookupProdOrderComp : 5407;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //LookupProdOrderComp : "Prod. Order Comp. Line List";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "RecalculateUnitAmount(PROCEDURE 38).ItemJnlLine1(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //RecalculateUnitAmount : 83;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //RecalculateUnitAmount : "Item Journal Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CheckWhse(PROCEDURE 39).Location(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CheckWhse : 14;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CheckWhse : Location;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "PostingItemJnlFromProduction(PROCEDURE 43).ProductionOrder(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //PostingItemJnlFromProduction : 5405;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //PostingItemJnlFromProduction : "Production Order";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "IsATOCorrection(PROCEDURE 10).ItemLedgEntry(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //IsATOCorrection : 32;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //IsATOCorrection : "Item Ledger Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "IsATOCorrection(PROCEDURE 10).PostedATOLink(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //IsATOCorrection : 914;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //IsATOCorrection : "Posted Assemble-to-Order Link";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "RevaluationPerEntryAllowed(PROCEDURE 45).ValueEntry(Variable 1001)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //RevaluationPerEntryAllowed : 5802;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //RevaluationPerEntryAllowed : "Value Entry";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "IsOpenedFromBatch(PROCEDURE 50).ItemJournalBatch(Variable 1002)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //IsOpenedFromBatch : 233;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //IsOpenedFromBatch : "Item Journal Batch";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "SubcontractingWorkCenterUsed(PROCEDURE 52).WorkCenter(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //SubcontractingWorkCenterUsed : 99000754;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //SubcontractingWorkCenterUsed : "Work Center";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ConfirmOutputOnFinishedOperation(PROCEDURE 79).ProdOrderRtngLine(Variable 1000)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ConfirmOutputOnFinishedOperation : 5409;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ConfirmOutputOnFinishedOperation : "Prod. Order Routing Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ItemJnlTemplate(Variable 1007)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ItemJnlTemplate : 82;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ItemJnlTemplate : "Item Journal Template";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ItemJnlBatch(Variable 1008)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ItemJnlBatch : 233;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ItemJnlBatch : "Item Journal Batch";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ItemJnlLine(Variable 1009)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ItemJnlLine : 83;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ItemJnlLine : "Item Journal Line";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "Item(Variable 1010)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Item : 27;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Item : Item;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ItemVariant(Variable 1012)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ItemVariant : 5401;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ItemVariant : "Item Variant";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "GLSetup(Variable 1014)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //GLSetup : 98;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //GLSetup : "General Ledger Setup";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "MfgSetup(Variable 1034)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //MfgSetup : 99000765;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //MfgSetup : "Manufacturing Setup";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "WorkCenter(Variable 1040)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //WorkCenter : 99000754;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //WorkCenter : "Work Center";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "MachineCenter(Variable 1039)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //MachineCenter : 99000758;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //MachineCenter : "Machine Center";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "Location(Variable 1048)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Location : 14;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Location : Location;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "Bin(Variable 1030)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //Bin : 7354;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //Bin : Bin;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ItemCheckAvail(Variable 1021)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ItemCheckAvail : 311;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ItemCheckAvail : "Item-Check Avail.";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "ReserveItemJnlLine(Variable 1022)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //ReserveItemJnlLine : 99000835;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //ReserveItemJnlLine : "Item Jnl. Line-Reserve";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "NoSeriesMgt(Variable 1023)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //NoSeriesMgt : 396;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //NoSeriesMgt : NoSeriesManagement;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UOMMgt(Variable 1024)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UOMMgt : 5402;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UOMMgt : "Unit of Measure Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "DimMgt(Variable 1027)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //DimMgt : 408;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //DimMgt : DimensionManagement;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "UserMgt(Variable 1033)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //UserMgt : 5700;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //UserMgt : "User Setup Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CalendarMgt(Variable 1041)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CalendarMgt : 99000755;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CalendarMgt : CalendarManagement;
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "CostCalcMgt(Variable 1042)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //CostCalcMgt : 5836;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //CostCalcMgt : "Cost Calculation Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "PurchPriceCalcMgt(Variable 1020)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //PurchPriceCalcMgt : 7010;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //PurchPriceCalcMgt : "Purch. Price Calc. Mgt.";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "SalesPriceCalcMgt(Variable 1025)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //SalesPriceCalcMgt : 7000;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //SalesPriceCalcMgt : "Sales Price Calc. Mgt.";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "WMSManagement(Variable 1026)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //WMSManagement : 7302;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //WMSManagement : "WMS Management";
    //Variable type has not been exported.


    //Unsupported feature: PropertyModification on "WhseValidateSourceLine(Variable 1032)". Please convert manually.

    //var
    //>>>> ORIGINAL VALUE:
    //WhseValidateSourceLine : 5777;
    //Variable type has not been exported.
    //>>>> MODIFIED VALUE:
    //WhseValidateSourceLine : "Whse. Validate Source Line";
    //Variable type has not been exported.

    var
        CLE: Record "Capacity Ledger Entry";
        PostedMatIssHdr: Record "Posted Material Issues Header";
}

